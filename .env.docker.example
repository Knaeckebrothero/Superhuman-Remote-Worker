# =============================================================================
# Graph-RAG Autonomous Agent System - Docker Environment Configuration
# =============================================================================
#
# Copy this file to .env.docker and update the values for your environment.
#
# Usage:
#   cp .env.docker.example .env.docker
#   docker-compose --env-file .env.docker up
#
# =============================================================================

# -----------------------------------------------------------------------------
# PostgreSQL Database
# -----------------------------------------------------------------------------
POSTGRES_USER=graphrag
POSTGRES_PASSWORD=graphrag_password
POSTGRES_DB=graphrag
POSTGRES_PORT=5432

# -----------------------------------------------------------------------------
# Neo4j Graph Database
# -----------------------------------------------------------------------------
NEO4J_USERNAME=neo4j
NEO4J_PASSWORD=neo4j_password
NEO4J_BOLT_PORT=7687
NEO4J_HTTP_PORT=7474

# -----------------------------------------------------------------------------
# LLM Configuration
# -----------------------------------------------------------------------------
# OpenAI API key (required)
OPENAI_API_KEY=sk-your-api-key-here

# Optional: Self-hosted LLM endpoint (e.g., vLLM, Ollama)
# LLM_BASE_URL=http://localhost:8080/v1

# Web search (optional but recommended)
TAVILY_API_KEY=tvly-your-key-here

# -----------------------------------------------------------------------------
# Service Ports
# -----------------------------------------------------------------------------
ORCHESTRATOR_PORT=8000
CREATOR_PORT=8001
VALIDATOR_PORT=8002

# -----------------------------------------------------------------------------
# Logging
# -----------------------------------------------------------------------------
LOG_LEVEL=INFO

# -----------------------------------------------------------------------------
# Citation Engine (if using external service)
# -----------------------------------------------------------------------------
# CITATION_DB_URL=postgresql://graphrag:graphrag_password@postgres:5432/graphrag
# CITATION_LLM_URL=http://localhost:8080/v1
# CITATION_LLM_MODEL=120b-instruct
# CITATION_REASONING_REQUIRED=low
