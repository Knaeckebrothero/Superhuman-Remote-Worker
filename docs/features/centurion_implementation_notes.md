---
tags:
  - feature
  - implementation
  - agent
  - orchestration
aliases:
  - centurion implementation notes
  - officer implementation plan
related:
  - "[[centurion]]"
  - "[[officer_knowledge_plane]]"
  - "[[officer_supervision_surface]]"
  - "[[officer_message_routing]]"
---

# Centurion — anchored implementation notes

Companion to [[centurion]]. Distilled from the 2026-07-28 code-planner research (three agents; all anchors verified against the working tree at `95ee3011` that day — re-verify on pickup, they will drift). This is the build plan; the design rationale lives in the feature doc.

> **2026-08-14 follow-on:** this file remains the as-built v1 record. Do not extend its
> historical S5/S6 tool list ad hoc. The next operating-surface work is split into
> [[officer_knowledge_plane]] (authoritative memory + no-object ceiling),
> [[officer_supervision_surface]] (truthful scoped reads + bounded evidence), and
> [[officer_message_routing]] (worker/officer/user routing + timeout liveness), all through
> [[unified_orchestrator_tool_surface]].

> **First live conference + the notify channel made two-way (2026-07-30→31,
> `0ccc5038` + `99223c6d`).** S9 got its first production exercise on Better Resavio:
> single-writer conference, hold + live stand-by notice, brief wake on conclude, and
> watchdog self-heal all worked mechanically — what failed was everything *around*
> them (eight findings, two fixed same-day: `docs/issues/officer_conference_live_fire_findings.md`).
> As-built deltas that matter for pickup:
> - **`notify_user` is now a two-way channel (P-H)**: calls render as first-class
>   "Officer → you" bubbles in the session log (never folded/auto-collapsed); page
>   emails append an "Open his log to reply" session link (bare URL — the email leg
>   escapes markdown); and pages persist a real notification row (they never had —
>   `message_log.thread_id` was `VARCHAR(12)` and `job_id` FK'd to jobs; migration
>   `0077` widens it, the inbox renders a "Session page" card routing to the
>   session). The session log IS the reply channel; IMAP reply routing stays
>   deliberately unbuilt.
> - **Deploy churn no longer rolls centuries (P-G.1)**: image identity is the last
>   input-touching commit per component, so docs/batched pushes stop re-tagging (and
>   drain-recycling) the worker fleet mid-job. Digest-based drift and sitrep
>   deploy-window tagging (F8) remain open.
> - **P1 mechanics held under real supervision** (first supervised night): the
>   guidance lane steered mid-run without re-plans, the officer attached
>   `required_deliverables` contracts unprompted on every dispatch, evidence push
>   preserved cancelled work (the salvaged RED tests seeded the next morning's
>   recovery job), and the slot roster's `backend: vm` carried the heavy workspace
>   all night. The seal gate itself is still unexercised live (jobs were cancelled
>   pre-completion).
> - **Command doctrine now lives in the KB**: the Legate's standing orders
>   (act-within-authority instead of idling the century; guidance over re-plans with
>   a one-re-plan cap; cancel only provably stalled work; suspect deploys on
>   clustered offline events) were written by the officer as a `decision` note —
>   the charter/KB injection surfaces them at every wake.

> **Steer semantics changed 2026-07-30 (P1 wave of
> `docs/issues/officer_blind_reads_and_worker_bureaucracy.md` — supersedes every
> "`urgent` interrupts" mention below).** `steer_worker_job(urgent=True)` no longer
> triggers a destructive resume: on a live run it appends to
> `context.pending_guidance`, rides the heartbeat response into the worker pod, and
> renders as a transient `[SUPERVISOR GUIDANCE]` pair on the very next LLM turn —
> intact context, no compaction, no re-plan; delivery is acked into
> `context.consumed_replies` (readable by the officer), and only a job with **no live
> run** falls back to a resume. The destructive path is now exclusively
> `resume_worker_job` (docstring prices it honestly). Non-urgent steers are delivered
> for real too: the boundary drain injects a persistent `[QUEUED MESSAGES]` message
> and consumes the thread (the old `messages/*_received.md` dead-letter box is fixed).
> The officer also gained `create_worker_job(required_deliverables=[...])` (P1-C):
> declare the contract at dispatch and let the platform's Gitea-backed seal gate
> bounce empty seals instead of burning a review cycle. The former agent-visible
> per-boundary status file was retired on 2026-08-03 after a smoke test proved its
> stale snapshot could deadlock worker completion; status is derived from the contract
> and repository ref instead. Cancel/pause now push an evidence snapshot before teardown
> (P1-D) — a cancelled worker's mid-phase work survives in its Gitea branch.

> **As-built S7–S9 + guards (2026-07-30 overnight, commits `ae045186` / `97f2d673` / `773ad46e` / `f7274da0`) — v1 complete.** Deltas from the plan below:
> - **S7 (`ae045186`)**: the "three lockstep sites" are FOUR — the `NoteTypeValue` Literal in `knowledge_tools.py` binds the LLM schema and `tests/test_tool_vocabularies.py` pins Literal==NOTE_TYPES==reindexer. One-active-charter is **app-enforced in kb_write**, NOT a partial unique index — the reindexer upserts human-editable files and a constraint would wedge a whole run on one duplicate; the write path also **fail-closes** when the uniqueness lookup errors. Worker denial keys on `ToolContext._thread_id` (set only by persistent sessions; `job_id` is useless — sessions set it to the thread id). The charter fetch (`KnowledgeStore.get_charter_note`) is project_id-keyed with **NO path filter** — lite sessions write pathless pgvector rows that `get_note_by_slug`/`list_notes` (files-canonical, `path IS NOT NULL`) can never see. Injection = a dedicated transient pair (`charter_inject_` prefix, registered in `is_workspace_injection_message` so summarization excludes it), prepended LAST so it sits absolutely first in the tail zone; gate `_charter_injection_enabled` = officer.enabled OR officer.conference, strict `is True`. Risk 11 fixed for real: kg-less+gitless kb_write now returns an error when the pgvector write (the sole store) fails. `OfficerConfig.conference` added (loader both paths + sanitizer bool key). Charter template ships beside the expert (`config/experts/centurion/charter_template.md`, loader-inert) + persona rule 9.
> - **S8 (`97f2d673`)**: migration **0075** (CHECK + budget relax, as planned). The advance branch keeps `consecutive_failures`/`last_error` bookkeeping for the sitrep and clears both pointers, but never writes `status` — a failing turn never stops an officer loop. The wake is `source='loop'`, dedup `<loop8>:<seq_index>` (exactly-once via the stage barrier + insert dedup; the completed member's own `job_transition` wake coalesces with it). Conversion is **one-way** (→ officer) via `convert_project_loop_to_officer` — guards in the UPDATE's WHERE (active row, `campaign IS NULL`, not already officer) so a raced campaign start can't slip between check and write; an in-flight TURN converts cleanly (its completion lands in the officer branch). Start-path: officer loops skip the first spawn and wake the officer (`started:<loop8>`); an enabled officer thread is required at start AND convert. Sweeper: ONE guard after the stage-sweep branch (empty pointers = steady state; adopt+heal both skipped); the stage-sweep stays as the missed-hook net.
> - **Guards (same commit)**: `daily_token_ceiling` enforced **at the drain**, not the turn loop — today's tokens from `usage_events` `ref_id=thread` (the audit materializer stamps `ref_kind='thread'` for session rows) vs the metadata ceiling; over → `defer_session_wake_events` (NEW primitive: back to pending with future `fire_at`, **no attempts burned** — release would bury rows at max_attempts) to next UTC midnight + one day-stamped digest notice (`officer_state.ceiling_notice`). Fail-OPEN when metering is down; direct Legate input bypasses the drain entirely. Quiet hours: `dispatch(bypass_quiet_hours=True)` on officer pages only.
> - **S9 (`773ad46e` + cockpit `f7274da0`)**: the hold lives at **`config_override.officer.hold`** — matching the S3-era claim-query predicate and watchdog check that were already wired for it (NOT `officer_state`; the S3 sitrep rule was about high-churn runtime state — the hold is a rare, config-shaped flag and two readers already agreed). Cleared by deep-merging `{"hold": None}` (JSON null → `#>>` yields SQL NULL → unheld for all three readers). Conference create = ordinary session create with `officer.conference` (one open per project → 409 with the existing id; needs one unambiguous project); hold stamped AFTER the metadata write + best-effort live stand-by notice into the officer pod. End hooks: `end_thread` AND the agent-status `'ended'` guarded branch both call `_conclude_conference_if_any` (release hold if owned by this conference + enqueue `source='conference'`, dedup = conf thread id). **Watchdog self-heal**: a held officer whose conference is gone/ended/**suspended** concludes on the next tick — attention-sleep parks idle conferences, and the officer must not be held all night; consequence: reattaching a suspended conference resumes UNHELD (residual; only the ended→`/resume` path re-holds). Dispatch fence: the job funnel 409s officer-attributed jobs while held (before slot admission). `GET /api/projects/{id}/officer` powers the cockpit card. Cockpit: Centurion tab in project-detail (provision form with slot-roster builder → navigates into the new session, attach = headless boot; status card with next-wake label, queued events, pages, ceiling state, kit chips, digest tail, conference + two-click retire) + officer/conference badges on session rows (from list metadata; absent metadata = no badge, graceful).
> - **k3d live (2026-07-30 ~23:00Z)**: both migrations verified applied (CHECK defs queried); full cycle green FIRST TRY — provision w/ kit → summary → conference → hold stamped (summary showed both) → rival conference 409 → end → hold released → `conference` wake row **sent** (durable branch; no live pod) → retire → plain-session create/delete regression green. Full pytest: 2228 passed, 1 pre-existing environmental failure (`test_database_phase1` local-connect). Cockpit: 1460 vitest + production build green.
> - **Follow-ups the same night**: `1968c082` — the S8/S9 wake payloads used a `note` key the sitrep renderer never surfaces; renamed to `summary` (what `_reason_lines` reads), matching the pre-existing fleet notifies. `9c92ed6e` — CI caught hardcoded `len(NOTE_TYPES) == 13` count/member assertions in `test_knowledge_graph.py` (the local "full" run had aborted at the environmental db-connect failure via `-x` before reaching the file — don't trust `-x` runs as full). `d197e2ec` — app-guide project-tabs list + focused loop guide gained the Centurion tab and an Officer-scheduling section; `loop_unified_engine.md` notes the third mode.
> - **Officer brain (`aa83fd30` + `ae1d0285`)**: the expert pins NO model on purpose (per-deployment catalogs — k3d has no codex-proxy; a pinned absent model would brick officers there), so an unset brain falls to the account/base default — observed live as the Resavio centurion running gemma-4-moe while commanding MiniMax-M3/sol workers. Setting surface: the provision form's "Officer's own model" picker + "Reasoning" select (create_thread bridges top-level `model`; NEW `reasoning_level` request field with `_validated_reasoning_level` — the create path rebuilds config_override from validated fragments, so an unbridged effort was silently dropped, same trap as the officer block); the summary endpoint + card show `model · effort`. Live re-brain procedure (performed on `d67ee261`): jsonb-merge `{"llm": {"model": ...}}` into the thread's `config_override`, delete the serving pod → graceful-'ended'→'suspended' → watchdog respawn (~7 min incl. cooldown; the kill was the documented silent shape) → attach hydrates the new config. Verified in the fresh pod's log: `Created Codex LLM: model=gpt-5.6-sol, base_url=…codex-proxy…, max_context_tokens=400000, reasoning=responses_api(effort=high)`. Effort resolution: `LLMConfig.reasoning_level` (default "high") → family matrix options (gpt-5.6: low…max) → factory clamp; account-level `default_reasoning_level` is the gap-filler layer.
> - **Item-7 sighting on dev**: the rollout left `persistent-d67ee261-334` in `Error` as debris while the watchdog had respawned him onto an idle-pool agent — the release-path pod-reaping gap is live behavior, not just a k3d note; cleaned manually.
> - **Open after v1**: officer digest ring visible in the card but no daily email sender; charter authoring is kb_write/kb_update (no dedicated UI); suspended-conference reattach re-hold; dedicated-pod debris reaping (item 7, now observed on dev); the S9 design's "single-select project picker replacing the legacy multi-project tap-in" was deliberately NOT built — the project-page tab + conference button covers the flow with far less regression surface.

> **As-built (S1+S2 + timer substrate, 2026-07-29, uncommitted):** implemented per this plan with four deltas — the migration landed as **0074** (0073 was taken by canvas snapshots; `project_loops` CHECK changes deferred to S8's own migration); the watchdog lives **in `orchestrator/main.py`** beside `attention_sleep_sweeper` (leader-gated), not a separate service file; the filing endpoint is `POST /api/agents/threads/{id}/officer/wake` (internal-key, clamps server-side); and all three officer gates use **strict checks** (`is True` / `isinstance(dict)`) because MagicMock configs in the pinned test files would otherwise trip them. Officer event drain rides `session_wake_sweeper_loop` (replica-safe claim, not leader-gated); minimal `[SITREP]` rendering ships now, replaced by the S3 sitrep service. Tests: `tests/test_officer_substrate.py` (20) + the three pinned files green; full suite green except a pre-existing local-Postgres connect test.
>
> **k3d smoke (2026-07-29) — PASSED, with six defects found+fixed in the run:**
> verified live: migration + outbox; officer flag persisted at create; single boot self-wake bootstrapping the lazy loop; completed turn; endpoint-filed timer **clamped 99→2** and honored at `fire_at` (delivered +6s); coalesced `[SITREP]` with payload reason; watchdog implicit `sleep_max` filing; watchdog **respawn of a dead-bound officer** (log `respawning officer thread … agent live=False`, fresh pod, boot wake); retirement (DELETE → `ended` + flag lowered, watchdog stands down); repeated wake cycles against a turn-crashing officer (bounded at tick rate — the S5 token ceiling is the designed brake).
> Defects found by the smoke, all fixed same-day: **(1)** `create_thread` rebuilds `config_override` from validated fragments and silently dropped the officer block → new `_validated_session_officer_override` (main.py, beside the workspace/tools validators). **(2)** *Pre-existing*: `_maybe_estimate_reasoning_tokens` let a tiktoken vocab-download failure kill every turn that captured reasoning (offline/split-DNS clusters) → metrics are now best-effort. **(3)** Boot self-wake fired per *attach*, duplicating on re-attach → gated on the loop not already running. **(4)** The watchdog respawned still-booting officers (readiness-pending looks dead) → boot-grace keyed on `metadata.agent_pod.created_at` (reuses the respawn cooldown as the window). **(5)** Implicit filing keyed on ai/tool age re-filed mid-turn (a turn's ai row lands only at turn END; observed re-file +22s after a delivery) → new `get_officer_last_engagement` (GREATEST of any transcript row and last timer `sent_at`). **(6)** Graceful pod termination (delete/drain/deploy) let the agent's shutdown handler write thread `ended` with the officer flag still raised → permanently dead officer, watchdog correctly standing down; agent-side 'ended' for officers now maps to **'suspended'** at the status endpoint (deliberate retirement still goes through `end_thread`, which lowers the flag; true SIGKILL crashes never reach the endpoint and take the watchdog's dead-pod path).
> Environmental note for k3d: the smoke ran with `sleep_min/max = 1/2 min` via `config_override.officer`; Tilt live-syncs `.sql` files without triggering a uvicorn reload (migrations apply on the next `.py`-change reload or pod restart), and same-tag agent-image pushes mean a spawned pod should be `grep`-verified for new code before trusting behavior.
>
> **SHIPPED + FIRST COMMAND (2026-07-29 night).** Everything below is merged and live: `daf00d80` (S1+S2), `64f51a91` (S3–S6), `6920a135` (CI fix: `_officer_cfg` must double-getattr — attach-path test FakeSessions carry no `.config`), `4276ae1e` (S5b slot roster) — CI green, `deploy-experimental` bumped fleet.yaml, Fleet rolled the dev cluster (`superhuman-remote-worker` ns on the homelab; orchestrator + agent on `sha-4276ae1`).
> - **S5b as-built (slot roster, user design)**: `orchestrator/services/officer_slots.py` — pure `validate_slots_spec` / `roster_from_meta` / `admit` / `capacity_lines` (unit-testable without the app; ValueError→400 at provision, `SlotAdmissionError`→409 at dispatch). Sanitizer admits + hard-validates `officer.slots`. Funnel admission sits **before the VM gate** (a `backend: vm` slot must stamp first so the submit-time 403 still fires), stamps slot model/backend ON TOP of body/project overrides via `_deep_merge_dicts`, records `jobs.context.officer_slot` (per-slot counts = one GROUP BY), replaces the flat gate (flat path preserved when no roster). Tool: `create_worker_job(slot=...)` rides `payload.context`. Sitrep capacity line per-slot. k3d live matrix passed first try (roster persisted; unnamed/unknown/full 409s listing free counts; stamped job verified). 61 tests in `tests/test_officer_slots.py` + suite.
> - **Dev provisioning recipe** (until the S9 toggle exists): the MCP's `create_persistent_thread` cannot pass `config_override` — provision via in-pod curl on the dev orchestrator: `POST /api/persistent/threads` with `X-Internal-Key: $MCP_INTERNAL_KEY` + `X-MCP-User-Id: <owner uuid>` (the `require_approved_user` internal path), body = config_name `centurion` + `workspace.backend: none` + the officer block with slots. Charge/messages inject via the serving pod's `/api/input` (`role: human`, resolved from `threads.agent_id → agents.pod_ip`).
> - **First command**: Better Resavio (`68137e29`, 242 jobs, 18 loop rows all stopped/completed/failed — no conversion needed), officer thread `d67ee261-334a-4315-ab7f-b1e0e7ba8765`, kit `line 2×MiniMax-M3/vm` + `heavy 1×gpt-5.6-sol/vm`, bounds 10/45, officer brain on the dev default model. First watch: oriented from the KB, filed `century-state-on-change-of-command` (engine stable 7/7 probes + 145/145 tests; the Demo Gap; F7/F8 doc drift; F-30-01 deprecation; "shipped claims are unverified"), queued a digest, monitored the leftover `Loop iter 3 · DEVELOPER` job without interfering, dispatched nothing, and asked the **Demo Definition question**. Legate answer delivered (web UI required; 2–3 real front-desk flows; one-command demo data start; one-week standing readiness; truth-first sequencing approved; heavy = UI architecture, line = build-out).
> - **Observability gap (S9's real shape)**: today the officer is watched via his session transcript (sitreps + sleep reasons ARE the log), KB state notes, and SQL for the pending timer + `officer_state.digest`. The cockpit needs: project-page enable/disable, officer badge, digest panel, next-wake display.
>
> **As-built S3–S6 (2026-07-29 evening, uncommitted):** built in one pass per this plan; k3d-smoked same night. Deltas and decisions:
> - **S3 sitrep**: `orchestrator/services/sitrep.py` — `build_wake_message(db, thread, rows) -> (text, state_patch)`; per-section try/except degradation; handles resolved lazily off `import main` (`audit_reader`, `usage_ledger`) with explicit-arg override for tests. Fingerprints/watermark live in `threads.metadata.officer_state.sitrep` (NOT `config_override.officer` — runtime state must not flow into config resolution) via new `postgres.merge_thread_officer_state` (atomic `||` at the officer_state key). On live inject the drain merges the patch; durable branch never advances (risk 5). A failed jobs section returns the PREVIOUS fingerprints so a transient DB error can't re-baseline everything as NEW. Steps come from `audit_reader.get_audit_counts` (batch), last-write from `get_audit_time_range`; stall flag = same step count across watermarks.
> - **S4 call sites**: collapsed to ONE choke point — `maybe_wake_session` itself now runs an officer leg first (`OFFICER_NOTIFY_STATUSES = TERMINAL + ('paused',)`; loads the job for `project_id`, `notify_officer`, kicks the drain) — every existing hook site gets officer coverage for free and the by-status sweeper backstop still applies. Only NEW hooks added: `pause_job` (calls maybe_wake_session with 'paused'), `sudo_gate._notify_project_officer` (both `new_request` SSE sites; sudo TTL caveat documented in the method), `stale_agent_detector` fleet notifies (steps 1/4/4b via `notify_all_officers`, `_step`-isolated). Double-wake suppression: `_deliver` converts an officer thread's jobs-outbox claim into a `job_transition` event with the SAME dedup key as the hook's enqueue (`_officer_job_dedup_key` = `<job8>:<status>`), so the two coalesce; conversion failure returns False (claim retried, never dropped).
> - **S5**: capacity gate in `POST /api/jobs` AFTER `creating_thread_id` resolution — internal path only by construction (the public path strips `thread_id`), advisory xact lock + count, actionable 409. `POST /api/agents/threads/{id}/officer/notify` (internal): log=no-op, digest=`officer_state.digest` ring (cap 50), page=`_dispatch_officer_page` (resolves owner email explicitly; thread UUID rides `job_id` — plain uuid column, no FK) budgeted by `officer_state.pages` day-counter; over-budget or undeliverable → downgrade-to-digest with `downgraded: true`. Watchdog respawn-failure now pages via the same helper, deliberately OUTSIDE the budget. Agent tools: `notify_user` beside sleep in `core/officer.py` (auto-appended with sleep when officer.enabled; borrows `_get_client` from orchestrator.jobs at call time); `steer_worker_job` (→ `POST /api/jobs/{id}/messages/officer/reply`, fresh `officer` message-thread key queues for the next strategic phase, `urgent` interrupts) and `get_stuck_jobs` (→ `/api/stats/stuck`) in `orchestrator/jobs.py`. **Loop guard** in the turn loop: per-turn action budget (wake=turn), warn at budget, force-end at 2× or a ≥5× repeated `(tool, repr(args))` fingerprint — enforcement ALWAYS after the tool batch (mid-batch break would orphan tool_calls → pairing 400); force-end files no sleep on purpose (watchdog's implicit sleep_max is the designed net). `daily_token_ceiling` remains config-only (parsed, NOT yet enforced — deferred; the guard + watchdog cadence bound the crash-loop case).
> - **S6**: `config/experts/centurion/{config.yaml,persona.txt}` — `$extends: session_base`, `workspace.backend: none`, officer defaults with `enabled: false` (the THREAD override enables; the same expert dresses S9 conferences), `tools.orchestrator` = the 9 job tools + steer + stuck, `research: []`/`citation: []` trimmed. Persona: charge-not-goal (five standing questions), klug-und-faul, wake protocol, ask-or-assume, delegation discipline, three-channel communication contract, one-mind-at-a-time. App-guide experts roster row added (pinned by `test_app_guide_content`).
> - **Smoke fixes riding along**: `_release_thread_resources` + `_suspend_thread_resources_inner` now run BOTH provisioners' cleanup (the old `elif` skipped persistent pod+PVC deletion whenever the pool provisioner existed — the item-7 10Gi-PVC leak; suspend deletes the pod but never the PVC); dedicated-pod `readinessProbe` moved `/ready`→`/health` (item 8 — `/ready` means "free for a session", 503 while serving).
> - Tests: `tests/test_officer_sitrep.py` (13) + substrate suite updated for the two-tool factory + probe test + endpoint inventory regenerated. Suite failures beyond mine reproduced on the clean baseline (mcp_manager/attach-cloud/db-connect — environmental with Tilt up).
>
> **k3d smoke S3–S6 (2026-07-29 ~19:10Z, project `1acdadc0`, officer thread `9ab37bde`)**: expert loads (officer defaults, backend none, 11 orchestrator tools); boot wake → first turn explored via get_session_context/kb/list_worker_jobs + project jobs and ended with `sleep(2, "Project is idle; awaiting instructions.")` — timer row upserted, fired at 19:15:27, **delivered +1s** with the FULL sitrep (first-sitrep header, reason echo, NEW job lines, in-flight detail with audit-DB `steps N, last write Xs ago`); notify endpoint live (digest→`officer_state.digest[1]`, page→`pages 1/3` day-stamped); capacity 409 at cap 1/1 with actionable detail (internal path; the PUBLIC path strips `thread_id` — first probe attempt via bearer token proved the stripping, not a gate failure); cancel hooks enqueued `job_transition` events — first delivered as an EARLY wake (19:16:28, before his pending timer: events beat timers), second held by the 300s per-source debounce and coalesced later. tiktoken cache seeded into the pod (`/tmp/data-gym-cache/9b5ad71b…`) to kill the ~12s/call DNS-retry stall — pod-local, lost on respawn; bake into the agent image someday.
> **Overnight watch (armed 2026-07-29 ~19:33Z, k3d):** officer thread `9ab37bde` (project `1acdadc0` "Century Smoke") left running with bounds 10/45, cap 3, charge injected as a human message (verify deliverables, restart wedged jobs, state-note + digest the morning report, page only if the century cannot work). Century state at handoff: `7d3e427c` failed (worker-side tiktoken DNS crash — pre-bake image; a restart on the baked image should succeed, which is his call to make), `7fee02e4` (workflow-engine comparison) processing on a baked-image worker. Observed before handoff: coalesced 3-reason fleet sitrep, digest queued via notify_user, `sleep(10)` acks. Morning review: thread transcript (sitreps + his notes), `metadata.officer_state` (digest ring + pages), KB notes `smoke-test-*`, and whether the failed job got restarted and verified.
>
> **Smoke defects 8+9 (same night):** the watchdog-respawned dedicated pod was **SIGKILLed mid-turn (exit 137) by its liveness probe** — `timeoutSeconds` defaulted to 1s, and tiktoken's vocab-download retries on the egress-blocked cluster are *synchronous* (~4s DNS timeout each, several per LLM call), blocking the event loop densely enough for 3 probe deadlines to lapse. Two-layer fix: dedicated-pod probes get `timeoutSeconds: 5` (+liveness `failureThreshold: 4`), and `docker/Dockerfile.agent.dev` pre-bakes the cl100k vocab (`TIKTOKEN_CACHE_DIR=/opt/tiktoken-cache`, populated at build where internet exists) so the retry storm never starts. The deeper defect — token counting doing blocking HTTP on the event loop — is real beyond k3d and deferred (worth an executor offload someday). Worker pods share the image, so their per-step stalls disappear too.
>
> **Smoke defect (7th of the feature):** `_boot_ws_watchdog` (persistent_app) killed the officer exactly 600s after boot — "No WebSocket connection … likely abandoned during creation" → `_terminate_session("boot_ws_timeout")`. Officers are headless by design; no WS is their steady state. Fixed with the `_officer_cfg()` gate at the top of the watchdog (+2 substrate tests). The S1/S2 smoke never saw it because every lifecycle scenario completed within 10 minutes of an attach — and the orchestrator watchdog's respawn masked the deaths as routine suspends. Corollary observed live: the respawn path + the item-8 probe fix produced a dedicated `persistent-<tid>` pod at **1/1 Ready** for the first time.
>
> **Late-smoke additions:** the lifecycle matrix completed green — dead-binding respawn ×2 (cooldown boundary honored to the tick: gate 18:09:19, fire 18:10:12), graceful-'ended'→'suspended' mapping (deterministic endpoint verification + live log), respawn-from-'suspended' (18:15:57), retirement ×3. Two **open S3-scope items** from the tail of the run: **(7)** `_release_thread_resources` does not delete watchdog-respawned dedicated pods (`persistent-<tid>` pods + their 10Gi `pvc-persistent-*` PVCs survived retirement; one premature-respawn orphan also ran 3h25m) — the officer respawn path must either register the pod where release finds it or the watchdog must reap pods for ended officer threads; **(8)** dedicated `persistent-<tid>` pods run 0/1-Ready while demonstrably serving turns — pre-existing readiness-probe quirk on the dedicated-pod path, worth a look because the watchdog's `probe_ready` liveness check tolerates it today only because `_resolve_live_agent` probes the port directly. Agent-side 'ended' reporting on graceful termination is racy (2 kills → 1 'ended', 1 silent) — both shapes are now handled, but don't build anything that assumes the report arrives.

## S1+S2 — Officer substrate + sleep tool (agent side)

### Turn lifecycle facts

- `run_persistent_loop` `src/persistent_graph.py:665`; outer `while True` :740; blocks on `callbacks.get_user_input()` :744; input items → `HumanMessage` :804–811 (`role` via `PERSIST_ROLE_KEY` :807, accepted roles `{human,event}` `persistent_app.py:276`; missing id minted :812).
- `_execute_turn` :1009; inner loop :1268; **turn ends on `not response.tool_calls` → break** :2127–2129; tools executed by direct `await tool.ainvoke(...)` :2220 inside `for` over tool_calls :2132; ToolMessage append+persist :2238–2245; **tool results otherwise feed straight back to the LLM** (:2277).
- Precedents: `TurnResult` :319–332 (`awaiting_permission` :332 — permission gate ends a turn mid-batch :2192–2197); **freeze-request seam** = the model for sleep: `request_workspace_upgrade` (`src/tools/core/upgrade.py:52–100`) sets `context.request_freeze` (`src/tools/context.py:783–793`), turn loop checks `consume_freeze_request()` at `persistent_graph.py:2269–2275`.
- `/done` is a WS method (`persistent_app.py:2964–2966` → `_handle_archive` :6240), not a tool. `INTERRUPT_SENTINEL` `persistent_graph.py:237`.

### Sleep tool design (decided)

Real registered tool + ToolContext flag (NOT a pre-dispatch sentinel — a real tool gets streaming/persist/pairing for free):

1. `src/tools/context.py:783–805` — beside freeze slot: `officer_sleep_request: Optional[dict]`, `request_officer_sleep(minutes, reason)`, `peek_officer_sleep()` (turn loop), `consume_officer_sleep()` (transport).
2. New `src/tools/core/officer.py` mirroring `upgrade.py:35–100` — `OFFICER_TOOLS_METADATA` (category `core` so `filter_tools_by_backend` `src/tools/registry.py:214–267` never drops it on `none`) + `create_officer_tools`: `@tool sleep(minutes, reason)` → clamp advisory, set request, return "sleeping for N minutes — events will wake you early."
3. Wire: `src/tools/core/__init__.py:23–51` (mirror upgrade lines :25/:37/:46/:51); `registry.py:428` extend no-workspace exemption with `OFFICER_TOOLS_METADATA`; `src/api/persistent_session.py:1541–1547` append `"sleep"` when `config.officer.enabled`.
4. `persistent_graph.py` — `TurnResult.ended_by_sleep: bool = False`; after the tool-batch `for` (between :2276 and :2277): `if tool_context and tool_context.peek_officer_sleep(): break`; return carries `ended_by_sleep=True`. Guard `tool_context=None` (tests build callbacks without one).
5. Optional belt: `persistent_app.py:4400–4409` `_loop_permission_check` early-APPROVE for `sleep` (side-effect-free) so it works outside `autonomous` mode.

### Wake timing — external durable timer (decided 07-29)

- `_loop_get_user_input` `src/api/persistent_app.py:3924–3999`: today `asyncio.wait_for(queue.get(), idle_timeout_minutes*60)` :3974–3978; on timeout broadcasts `session.idle_timeout` + **raises `IdleTimeoutError`** :3996 → `_loop_completion_handler` :4976 (idle branch :4990–4996) → `_handle_idle_archive` :6340 → `_terminate_session("idle_timeout")` :2102 (writes `'ended'` :2212–2213). **Idle timeout is terminal; officer branch replaces it.**
- **The timer is orchestrator-fired and PG-durable** (user decision — pod crash / node downtime must not lose the schedule; next pod picks up where it was). Mechanics:
  - On turn park with a pending sleep request, the transport consumes it and **files the wake**: `POST /api/persistent/threads/{tid}/officer/wake {fire_at, reason}` (internal-key) → upsert of the `timer` outbox row (`source='timer'`, fixed `dedup_key='timer'`, `ON CONFLICT (uq_swe_pending) DO UPDATE SET fire_at` — the one source with update-not-ignore semantics). The tool itself stays a pure flag-setter.
  - The event drain claims due rows via `fire_at <= now()` and injects `[timer wake]` like any other event. In-house precedent: `orchestrator/services/cron_dispatcher.py` (`cron_dispatcher_loop` :63 tick, `_process_one_due_automation` :120 — `next_run_at` + FOR UPDATE SKIP LOCKED claim).
  - **Implicit `sleep_max`**: the transport files nothing when the turn ends without a sleep call; the **watchdog files the default** — unheld officer thread with no pending `timer` row and last ai/tool activity > `sleep_max` → upsert `timer` row `fire_at=now()`. This rule also covers transport-POST failures uniformly.
  - Agent-side the officer branch reduces to a **backstop**: `wait_for(queue.get(), max(2*sleep_max, 2h))`; on timeout **return** `{"content": "[backstop wake] orchestrator timer overdue", "role": "event"}` — never raise. No local deadline arithmetic, no module global, nothing to clear on terminate. Covers the partial failure "drain/watchdog broken, API up"; persona rule for a backstop landing during a held conference: standing hold + no brief yet → sleep.
- Skip both `awaiting_user` flips when officer: input-park `should_flip` :3952–3963 and sudo-gate flip `_loop_permission_check` :4478–4482. Eager/polite: neither auto-continues (modes only control the flip); polite+officer contradictory → officer bypasses both regardless of mode.
- Suppress the per-park `"ready"` NATS mirror for officers (`emit_session_event` :310–321 or broadcast site :3937; `_NOTIFICATION_METHODS` :257–266 → `session.waiting` SSE via `orchestrator/services/nats_bridge.py:819–826`) — else ~48 feed events/day.

### Config plumbing (decided)

- `OfficerConfig` dataclass beside `HeadlessConfig` (`src/core/loader.py:1827–1837`), field on `AgentConfig` :1893; parse in **both** `load_agent_config` (:2382–2390) and `load_agent_config_from_dict` (:2617–2624); add `"officer"` to **both** `known_fields` sets (:2351–2372, :2604). One shared parse helper — split-brain between file-boot and dict-boot is a real risk.
- Schema: `officer: {enabled: false, sleep_min_minutes: 5, sleep_max_minutes: 60, max_concurrent_workers: 3, max_pages_per_day: 3, max_actions_per_wake: 10, daily_token_ceiling: <n>}`.
- **Hard constraint**: officer-ness must ALSO be stamped into `threads.metadata.config_override.officer` at thread creation — orchestrator sweeps are SQL over metadata (pattern `orchestrator/main.py:24909`) and cannot see resolved expert config. Standardize one predicate: `COALESCE(t.metadata->'config_override'->'officer'->>'enabled','false') = 'true'` (constant near :24860). Config flows to the pod via existing `resolved_config` fetch (`_resolve_session_config` `main.py:1556–1679`; merge order session_base → account defaults :1516–1553 → expert → project → `metadata.config_override` :1590–1594; grant PDP `src/core/capability_grants.py:147` ignores unknown keys — cannot brick dispatch).

### Orchestrator exemptions (decided)

- Attention-sleep sweeper WHERE `main.py:24902–24921` (sweeper :24868): add officer exclusion. (`none`-backend threads already fail suspension at `workspace_suspension.py:504–505` — no ssh_host — but churn in `awaiting_user`; exempt explicitly.)
- `mark_orphaned_threads_ended` `orchestrator/database/postgres.py:5904` (SQL :5926–5930; caller `main.py:774–787`): exclude officers — otherwise a dead officer pod flips the thread `'ended'` in ~4 min. Watchdog owns officer lifecycle. Leave `mark_orphaned_threads_suspended` (:5936, SQL :5967–5971) unchanged (agent_id already NULL post-drain; officer never enters `awaiting_user`).

### Watchdog + respawn (decided)

- **The loop starts lazily** — only on first REST input (`handle_api_input` → `_ensure_persistent_loop_started("rest_input")` :2663) or WS attach (:2838). A respawned pod restores the session (`_attach_session` :1430 → `_restore_session_messages` **:5124** (def; called :2080) → status active :2083 → loop primitives :2090) and then parks with **no running loop**. Durable wakes restore as history rows (:5075–5087), not queue input.
- **Boot self-wake** (loop bootstrap + doc's respawn wake in one): end of `_attach_session` (after :2097), if officer → `_ensure_persistent_loop_started("officer_boot")` + `_accept_user_input("[wake: session started/restarted]…", role="event")`. Covers dedicated boot (lifespan :1041) and pool attach (`POST /session/attach` :2413).
- New `orchestrator/services/officer_watchdog.py`, leader-gated beside `main.py:7459–7461`, ~60s tick: select officer threads status IN (`active`,`suspended`); liveness via `session_wake._resolve_live_agent` (:313–351) + `probe_ready` (`services/session_lifecycle.py:77`); **turn-activity via `MAX(created_at) FROM thread_messages WHERE role IN ('ai','tool')`** — NOT `threads.last_activity` (bumped by every row incl. durable notices, `postgres.py:6252–6261`). Two timer duties (07-29): **file the implicit `sleep_max` timer** (upsert `timer` row `fire_at=now()`) when an unheld officer has no pending `timer` row and turn-activity is older than `sleep_max`; and treat a pending `timer` overdue past `fire_at + grace` with a live pod as a **delivery failure** → force-inject `[watchdog] overdue wake` direct to pod `/api/input` (the `_inject_live` pattern; deliberately bypasses the orchestrator turn lock :24024). Inject failed / agent offline / `suspended` → respawn via `_reprovision` split (idle pool `_find_idle_persistent_agent` :3167 / dedicated pod :23383–23386; `main.py:23292–23400`), **rate-limited one attempt per thread per N min**; page owner via NotificationService on repeated failure. Env: `OFFICER_WATCHDOG_INTERVAL_S`, `OFFICER_WAKE_GRACE_MINUTES`.
- **Deploys drain-suspend parked sessions** (drain intent `persistent_app.py:491–529`) — `suspended` is the officer's routine down-state; respawn-from-suspended is the common path.
- **Retirement**: `end_thread` `main.py:23062` additionally writes `officer.enabled=false` so the watchdog stands down. Crash-`ended` (`_terminate_session("loop_crash")` :2212–2213/:5003) with flag still true → **page, don't auto-resurrect** (pending Legate confirmation).

### Blast radius / tests

- `tests/test_idle_timeout.py`, `tests/test_attention_sleep_phase5.py` (:63, :147–177), `tests/test_headless_polite_phase6.py` (:97, :151–216) pin `_loop_get_user_input` — officer default-off keeps them green; add officer siblings.
- dual_app extract-delegate rule (`headless_persistent_sessions.md:450`): zero new agent routes — sleep/wake ride `/api/input` (dual_app delegates :1174); all new logic module-level.
- Stale anchors found during research: `_restore_session_messages` is :5124 (not :2379); `create_agent_pod` def :157.

## S3 — Event outbox + sitrep (orchestrator side)

### Why a new table

`maybe_wake_session(db, job_id, terminal_status)` (`orchestrator/services/session_wake.py:113`) is a **jobs-row outbox**: guarded `UPDATE jobs SET wake_state='pending'` (`mark_job_wake_pending` `postgres.py:4970–5010`) gated on `wake_on_complete AND created_by_thread_id IS NOT NULL AND status ∈ {completed,failed,cancelled,pending_review}` (`TERMINAL_STATUSES` :73 — **no `paused`**; columns `schema_current.sql:976–980`, partial index :3084). It cannot represent jobs the officer didn't create, `paused`, sudo, or fleet events. Reusable: `_resolve_live_agent` :313, `_inject_live` :354–386 (POSTs `{"content", "role": "event"}` to pod `/api/input` after `probe_ready`), `_deliver_durable` :394 (transcript row + claim settle), claim-then-send discipline, sweeper `session_wake_sweeper_loop` :631 / `kick_drain` :156.

**`role='event'` is transcript-only — the model sees a plain HumanMessage** (`persistent_app.py:2549–2631` persist-then-enqueue; restore comment :5075–5087). Mid-turn injection queues; **one queue item = one paid turn** (no coalescing exists anywhere). The orchestrator turn lock (`_ensure_thread_turn_lock` :23594–23617, in-process per-replica) is bypassed by direct pod POSTs — benign by design (dedup is the DB claim).

### Migration `session_wake_events` (app migration, next free number)

```sql
CREATE TABLE session_wake_events (
    id BIGSERIAL PRIMARY KEY,
    thread_id uuid NOT NULL REFERENCES threads(id) ON DELETE CASCADE,
    project_id uuid,
    source text NOT NULL,      -- 'job_transition'|'sudo_request'|'fleet'|'loop'|'respawn'|'conference'
    dedup_key text NOT NULL,   -- '<job8>:<status>' | 'sudo:<id>' | 'fleet:<step>'
    payload jsonb NOT NULL DEFAULT '{}'::jsonb,
    state text NOT NULL DEFAULT 'pending' CHECK (state IN ('pending','sending','sent','dead')),
    attempts int NOT NULL DEFAULT 0,
    fire_at timestamptz,               -- NULL = fire on next drain; future = durable timer (source='timer')
    claimed_at timestamptz, sent_at timestamptz,
    created_at timestamptz NOT NULL DEFAULT now()
);
CREATE UNIQUE INDEX uq_swe_pending ON session_wake_events (thread_id, source, dedup_key) WHERE state='pending';
-- claim predicate adds: AND (fire_at IS NULL OR fire_at <= now()); source='timer' upserts fire_at ON CONFLICT (others DO NOTHING)
CREATE INDEX idx_swe_claim ON session_wake_events (created_at) WHERE state IN ('pending','sending');
CREATE INDEX idx_swe_debounce ON session_wake_events (thread_id, source, sent_at) WHERE state='sent';
```

Same migration: extend `project_loop_scheduling_known` CHECK with `'officer'` (`schema_current.sql:1304`); relax `project_loop_has_budget` (:1302) with `OR scheduling='officer'`. Optional partial index on officer threads.

### DB + service methods

- `postgres.py` — mirror the jobs-outbox trio (:4970–5142): `enqueue_session_wake_event` (ON CONFLICT DO NOTHING), `claim_pending_session_wake_events(limit, visibility_timeout, debounce_by_source)` (FOR UPDATE SKIP LOCKED; debounce = NOT EXISTS recent `sent` same `(thread_id, source)`), `finish_/release_/gc_session_wake_events`; `get_officer_thread_for_project(project_id)` (threads.project_id `schema_current.sql:2015`, idx :3392) + `list_officer_threads()`; `count_active_thread_jobs(thread_id)` (terminal set as `get_thread_job_counts` :5165–5198); `merge_thread_officer_state` (jsonb merge modeled on `merge_workspace_container_context` :3360).
- `session_wake.py` — factor delivery; add `notify_officer(db, project_id, source, dedup_key, payload)` (never raises; no-op without officer) + `notify_all_officers`; `drain_pending_event_wakes`: claim → group by thread → `sitrep.build_wake_message` → one `_inject_live` per thread → live success: `finish` + advance watermark; durable fallback: compact reason-only notice, `finish`, **do NOT advance watermark**. Extend sweeper loop + kick to drain both queues.
- **Double-wake suppression**: in `_deliver` (:265), if target thread is officer → enqueue `job_transition` event and consume the jobs-outbox claim WITHOUT injecting `[JOB_FINISHED]`. Jobs outbox stays the durable trigger (incl. its by-status backstop arm `postgres.py:5072–5075`); event outbox is the single officer delivery channel.

### Sitrep data sources (verified)

| Field | Source | Trap |
|---|---|---|
| Job transitions | `jobs` status/`completed_at`/`failed_at` (0072; schema:981) via `idx_jobs_project_id` :3063, **snapshot-diff vs stored fingerprints** | `updated_at` poisoned: trigger :3693 fires on gc FK cascades (documented schema:1049) and wake-state writes |
| Steps / phase / last write | **audit DB** `agent_audit` (audit/0001:116–196): `COUNT(*) WHERE event_phase='pre'`, `MAX(timestamp)`, `MAX(phase_number)` per job | `get_job_progress` (`postgres.py:3735–3780`) is a **stub** (`progress_percent: 0` hardcoded) — do not build on it |
| Liveness | `agents.metadata->>'graph_progress'` + `graph_progress_seen_at` (heartbeat `main.py:20943` → `postgres.py:4214`; stall-sweep consumer :6027–6038) | |
| Tokens | **audit DB** `usage_events` (`ref_id`=job, audit/0002:46–112) or `llm_requests.metrics` | `jobs.total_tokens_used` is a **dead column** (zero writers) |
| Fleet | `agents` GROUP BY status (schema:197–216) | |
| Capacity | `COUNT(*) FROM jobs WHERE created_by_thread_id=$1 AND status NOT IN (…)` vs metadata cap | |
| Pending | `sudo_approval_requests` (schema:1653; sudo TTL 300s :1668, vm 24h) JOIN jobs for project; `thread_permission_requests` (schema:1971) for the officer thread | |
| Budget | `usage_ledger.query_usage(from, to, scope_project_id)` (`services/usage_ledger.py:272–340`; singleton `main.py:7150`) | |
| Watermark | `threads.metadata.officer.sitrep = {watermark, fingerprints, prev_fingerprints}` via `merge_thread_officer_state` | Advance ONLY on live delivery |

~8 indexed queries, low tens of ms; 300–900 tokens at ~10 jobs (tier: full for active/stuck, one-liners healthy, cap transitions ~20). Every section in its own try/except (stale-detector `_step` isolation pattern `main.py:687–702`); audit-DB optional at startup (`main.py:7082–7101`) — degrade per-section.

## S4 — Wake call sites

| Event | Anchor | Note |
|---|---|---|
| completed/failed/cancelled/pending_review (agent report) | `complete_job` hooks `main.py:15105–15106` (keyed on `new_status`; loop advance :15086 runs first — enqueues coalesce) | include `paused` here for freeze-completions (~:14900–15020 freeze arm) |
| cancelled (cockpit) | :9130 | |
| pending_review→completed (approve) | :11136 | |
| critic-approval / escalation terminals | :11502 / :11537 | |
| paused (user/agent pause) | `pause_job` :9144+ after status write | |
| sudo pending | `sudo_gate.py` after :157 (alongside `_broadcast_sse("new_request")` :170); project via `db.get_job(job_id).project_id`; vm-upgrade insert `main.py:14958` | 300s sudo TTL expires before a down officer sees it — human notification remains the fallback; don't promise sudo coverage from a dead officer |
| fleet: agents offline / orphans recovered | `stale_agent_detector` step 1 :707–715, step 4 :807–814 (count only), step 4b :821–832 (job ids) → `notify_all_officers`, `dedup_key='fleet:<step>'`, 10-min debounce | |
| session permission gates | **no orchestrator write site** — agent INSERTs `thread_permission_requests` directly (`persistent_app.py:4374+`) | defer; sitrep pending count covers |

## S5 — Toolset + capacity (verified inventory)

- `src/tools/orchestrator/` = **26 tools / 5 modules** (`__init__.py:12`): jobs.py 9 (metadata :25; `create_worker_job` :493 → `POST /api/jobs` :587 with `payload["thread_id"]=context.thread_id` :568; errors relayed verbatim :610–611; `_TRANSPORT_DENY` :171 rejects config_override carrying api_key/base_url/env_keys), projects.py 2, repositories.py 3, catalog.py 9, workflows.py 9 (incl. `get_project_loop` :744). Auth: `_get_client` :139 — `X-Internal-Key` + `X-MCP-User-Id` (docstring :146–156). **Category ships empty** (`config/session_base.yaml:117`); registry instantiates only requested names (`registry.py:693–702`); no shipped expert enables it → `centurion.yaml` must.
- **Missing wrappers (~30 lines each)**: steer → endpoint `POST /api/jobs/{id}/messages/{thread_id}/reply` `main.py:9793` (`require_job_access` :9807; `_route_inbound_reply` :9640; `append_queued_reply` `postgres.py:1965`; worker consumes `src/graph.py:3413`/:3542); stuck/fleet → `GET /api/stats/stuck` `main.py:17865–17867`.
- **`notify_user`**: does not exist (`docs/features/notify_user_tool.md:21` = design phase; zero code hits). `send_message` can't be repointed: sessions ship `communication: []` (`session_base.yaml:137`), endpoint 404s without a jobs row (`main.py:9300–9302`). Build: thread-scoped endpoint → `notification_service.dispatch()` (`notification_service.py:93`; quiet hours :563; existing job rate-limits :9384–9431 as precedent). Page budget/urgency per feature doc §6.
- **Capacity**: enforce in `POST /api/jobs` (`main.py:8368`) after `creating_thread_id` resolution :8668–8670 (persisted as `created_by_thread_id` + `wake_on_complete=true` for roots :8687–8688; column from migration 0070, comment schema:1025–1028): load thread → if officer → `count_active_thread_jobs` under `pg_advisory_xact_lock(hashtext(thread_id))` (TOCTOU) → 409 actionable detail. Optional `context.officer_created=true` for cockpit badging (automation precedent: `services/automations.py:138–143`).

## S6+S7 — Expert config, charter, KB types

- KB tools under `none`: **work** — `filter_tools_by_backend` drops only shell/browser/git + workspace/canvas (`registry.py:214–268`; `ScratchBackend.supports_file_tools=False` `scratch.py:59`); real gate is `context.knowledge_store` (`registry.py:638–639`), built vector-DB-only (`persistent_session.py:1097`; early-return without bindings/projects :1088). Dual-write **silently skipped** without git (`_dual_write_note` `knowledge_tools.py:499`, early return :511; lite path sets `git_versioning=False` `persistent_session.py:454`) → **pgvector write is the only durable write and is warn-only** (:1156–1158) — consider hard-fail for officer sessions. Sessions enable 10/12 kb tools (`session_base.yaml:104–115`; missing `kb_lint`/`kb_index` — fine for v1).
- Injection seam: `_inject_context_pairs` `persistent_graph.py:87` (anchor `workspace_injection.py:41`; call site :1391; retrieval assembly :1070–1266) injects manager/memory/knowledge/citation blocks. **Both KB routes are top-5 relevance-ranked** (`KbNotesRetriever` `src/services/memory/plugins/legacy.py:115`, hybrid_search :149–155; bindings route `src/core/knowledge_injection.py:95` at :1178) — charter needs a **fifth unconditional block** fetching `(project_id, note_type='charter')`. No pinned flag exists on notes.
- `charter` + `report` note types: vector migration (next free number; model on `vector/0013_kb_backlog_ticket_types.sql` + `0014_….notx.sql`) editing CHECK `valid_note_type` (`vector_schema.sql:492–497`); **three lockstep code sites**: `src/services/knowledge_graph.py:46` `NOTE_TYPES` (canonical; enforced `knowledge_tools.py:1096`), `orchestrator/services/kb_reindex.py:71` `VALID_NOTE_TYPES` (unknown → silently rewritten to `learning` :87/:235), plus the migration. One-charter-per-project = `(project_id, note_type)` via `uq_knowledge_project_note` (:491).
- Backlog lifecycle: create via `kb_write` (`_TICKET_TYPES` `knowledge_tools.py:87`; default `status='active'` :1171) → `fetch_backlog` (`project_backlog.py:56`; cap 20 :53–54; index `vector_schema.sql:517–519`) → injected at the single loop-kickoff funnel `main.py:12897–12916` → closed by **exactly one caller** `main.py:13635–13641` (campaign disposition; `close_backlog_ticket` `project_backlog.py:224` mirrors Gitea file + index row). Officer mode: officer closes tickets after §5 verification; define officer "in progress" convention (campaign-derived today, docstring :10–13).
- RecallStore in sessions: `capture(turn_end)` `persistent_graph.py:855–866`; `PersistentIntervalExtractor` `legacy_writers.py:108` (interval gate :126–130); `observer_interval: 5` + `project_scoped: true` `session_base.yaml:167–169`; pre-compaction :1297–1307; teardown `persistent_app.py:6267`/:6364; store built `persistent_session.py:2026–2037`.
- Recon shape: `config/experts/curator/config.yaml:9–11` (KB-writing deliverable, KB-only toolset :42–43); no `report` note_type exists yet (13 types `knowledge_graph.py:46–64`; closest `retrospective`/`state`).
- `threads` facts: `project_id` (schema:754–758 / schema_current:2015), `metadata` keys in use: workspace_container, vm, snapshot, config_override, datasource_ids, expert_id, project_ids (legacy), cloud, protected_cloud(_error), build_sha; merge helpers `postgres.py:3360/:3496/:3568/:3607/:3693`; redaction `main.py:8320`. No `officer` collision (grepped).

## S8 — Scheduling mode `officer`

- `scheduling` read sites: `main.py:13747` (`_rotate_loop_to_next_stage` campaign step); `services/project_loops.py:774`/:894 (campaign kickoff / loop_plan grant in `create_loop_job` :791); `routers/project_loops.py:67` (pattern `^(standard|campaign)$`), :125, :185; `postgres.py:12769/12793/12816` insert; **:12893 deliberately non-updatable**. CHECKs: `project_loop_scheduling_known` schema:1304; `project_loop_has_budget` :1302.
- **Branch**: in `_advance_loop_member` (`main.py:13929`), after per-member merge/retro :13972 and `_notify_loop_user_questions` :13983, **after winning `claim_project_loop_stage_barrier` :13986** (= exactly-once wake guard), before stop/bookkeeping :13994–14061: update loop counters (consecutive_failures/last_error for sitrep) → `notify_officer(project_id,'job_transition', f"{job_id}:{status}")` → `kick_event_drain()` → return. Bypasses: `remaining_iterations` decrement + `_loop_stop_reason` (:13275), cooldown park :14027, `_rotate_loop_to_next_stage` :13712 (incl. campaign :13747 and **KB-TTL decrement :13772** — officer projects lose cycle-based TTL ticking; documented trade-off). Also guard `_resume_project_loop` :14064 (re-runs advance; dedup key makes it safe).
- **Sweeper** (`project_loop_sweeper.py:119`): stage-sweep (:130–131 → `_sweep_stage` :182) **keep** — safety net; heal (:160–179 → `_heal_wedged_loop` :326; `HEAL_GRACE_SECONDS=600` :84; restore `heal_project_loop_stage` `postgres.py:13007–13031`) **must skip officer** — else all-terminal stage restored → barrier re-fires → duplicate wake every ~10 min forever. Guard both the legacy pointer-adopt :134 and heal :164 branches.
- **Start/convert**: `start_project_loop` (routers :74) always spawns `roles[0]` :194 — for officer skip first spawn, write empty pointers, require officer thread (400 otherwise). Conversion endpoint `POST /api/projects/{id}/loop/scheduling`: editor+; require officer thread; reject mid-flight campaign; guarded UPDATE (extend `_PROJECT_LOOP_UPDATABLE_FIELDS` `postgres.py:12873–12895` with superseding comment, or dedicated raw-SQL method).

## S9 — Conference surface

The conference (feature doc §2) is a **normal interactive session wearing the officer's identity** — nearly everything attaches for free because officer state is project-keyed:

- **Creation**: standard session-create with `expert=centurion` + the project bound; `config_override` sets `conference: true` and may override the workspace backend (centurion.yaml defaults to `none`; a conference can pick a workspace/VM tier — the existing tier machinery). `officer.enabled` stays **false** on the conference thread: no sleep tool, no watchdog, normal idle-archive — it's an ordinary session with his persona and stores.
- **Identity attach**: charter/KB/backlog/RecallStore all key on project (`persistent_session.py:1097`/`:2026–2037`; charter block from S7). **Gate the charter-injection block on (project + (officer.enabled OR conference flag)), not `officer.enabled` alone** — conferences must get the charter. Delegation + kb toolsets come from the centurion expert config.
- **The hold**: on conference create, stamp the background officer thread's metadata (e.g. `officer.hold = 'conference:<conf_thread_id>'`) and inject a live `[conference started — stand by]` notice. With the external timer the hold is uniform: (a) the event drain skips held threads entirely — event AND `timer` rows stay `pending`; the watchdog also skips filing the implicit-`sleep_max` timer for held threads; (b) a light **mechanical fence** — job-mutation endpoints reject requests attributed to a held officer thread with a legible 409 ("conference in progress") — stays as belt-and-suspenders. The only agent-local wake left is the long backstop (~2h+); if it fires mid-conference the standing hold notice covers it (persona: hold + no brief yet → sleep). Conferences are hours, not weeks, so post-compaction amnesia of the hold is a non-issue in practice.
- **Conference end** (archive or idle): existing teardown extractors (`persistent_app.py:6267`/`:6364`) + session summarizer produce the **brief**; clear the hold; `enqueue_session_wake_event(officer_thread, source='conference', dedup_key=conf_thread_id, payload={brief})`; kick the drain — the brief coalesces with everything held.
- **One-open-conference-per-project guard**: creation checks for an existing active conference thread for the project (metadata query) → reattach instead of duplicating (single-writer rule).
- **Cockpit**: single-select project picker (narrowing the legacy `metadata.project_ids` multi-tap-in, superseded per `postgres.py:5752`) routes "Talk to the centurion" → open/create the conference; officer badge on his log thread; event-role collapse rendering (`role='event'` rows + `[SITREP]` bracket tag); project page: Centurion enable/disable → provision (create officer thread + stamp metadata) / retire (`end_thread` + `officer.enabled=false`).

## Sequencing & risks (merged)

**Order**: S1+S2 (agent substrate) → S3 (outbox+sitrep) → S4/S5/S6/S7 parallel → S8 last; S9's brief-wake needs S3, its cockpit half is independent. Capacity (S5) is independent after S1's metadata stamp.

**Top risks**:
1. Double-wake officer-created jobs (both outboxes) — closed by the `_deliver` suppression; without it every delegation costs two turns per completion.
2. Sweeper heal loop on officer projects — closed by the skip guard; evidence chain `project_loop_sweeper.py:160–179/:326–404` + `postgres.py:13007–13031`.
3. Watermark on `updated_at` — snapshot-diff only (schema:1049 documents the trigger poison).
4. `_loop_get_user_input` regression (three test files pin it) — officer branch fully separate, default off.
5. Durable-delivery ≠ wake — watermark must not advance on durable branch; respawn wake re-covers.
6. `paused` must NOT enter the legacy outbox's `TERMINAL_STATUSES` (would spam ordinary sessions and corrupt `wake_notified_status` for redispatchable pauses).
7. Wake storms — insert dedup + per-source debounce (≥5 min; fleet 10) + per-thread coalescing + global throttle; residual: two replicas can claim disjoint batches (SKIP LOCKED) → at most two messages per burst (acceptable; later per-thread advisory lock in the drain).
8. Cross-DB sitrep fragility — per-section isolation; audit DB optional (`main.py:7082–7101`).
9. Capacity TOCTOU on parallel tool calls — advisory xact lock.
10. Officer resurrect-vs-retire — `officer.enabled=false` on deliberate end; crash-ended paged (pending decision).
11. pgvector warn-only KB write = silent data loss for officer notes — consider hard-fail (or retry) when `officer.enabled`.
12. Timer partial failures (07-29 external-timer model) — transport crashes before filing the wake → no `timer` row exists, which is exactly the watchdog's cue to file `sleep_max`; orchestrator timer path (drain/watchdog) broken while the API is up → the agent-side backstop (max(2×`sleep_max`, 2h)) preserves liveness with a distinctly-labeled `[backstop wake]`.

**Test matrix (minimum)**: sleep-tool turn-break unit; officer input-wait deadline/synthesis (pattern `test_attention_sleep_phase5.py:147`); loader parse both paths; sweeper-exclusion SQL; watchdog decision matrix; outbox enqueue-dedup/claim/debounce/grouping; officer completion → exactly one `[SITREP]` turn (and no `[JOB_FINISHED]`); officer loop advance leaves pointers empty across two sweeper ticks (no heal, no double wake); capacity 409 at cap; sitrep sections survive audit-DB outage; regression: `test_idle_timeout.py`, `test_attention_sleep_phase5.py`, `test_headless_polite_phase6.py` untouched green.
