name: main

# Full CI/CD pipeline for the main branch.
#
# Every push to main runs ALL lint, tests, and builds (not change-based).
# Lint and test failures block builds. On success: all images are rebuilt
# with :sha, :latest, and :vX.Y.Z tags, the Helm chart is published to
# OCI under its real name (superhuman-remote-worker), and a git tag is pushed.
#
# PRs to main run the full pipeline INCLUDING image builds, but skip the
# registry push (no login, no version tagging, no chart release). This
# catches Dockerfile / Packer breakages before they reach main and block
# the release process. Trade-off: PR feedback is slower, but a broken
# build post-merge is far more expensive than waiting for the build pre-merge.

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}
  # Lowercased repo path for GHCR cache refs. github.repository preserves
  # GitHub's case-sensitive name (e.g. Knaeckebrothero/Uni-Projekt-Graph-RAG),
  # but GHCR rejects uppercase image paths. docker/metadata-action handles
  # this for production tags; YAML expressions can't lowercase, so we hardcode.
  # Update this if the repo is renamed.
  REPO_LOWER: knaeckebrothero/uni-projekt-graph-rag
  SRW_SOURCE_URL: https://github.com/knaeckebrothero/Superhuman-Remote-Worker
  SRW_DOCUMENTATION_URL: https://github.com/knaeckebrothero/Superhuman-Remote-Worker/tree/main/docs

jobs:
  # ---------------------------------------------------------------------------
  # Lint — blocking on main (code should already be formatted from develop)
  # ---------------------------------------------------------------------------
  lint:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install ruff
        run: pip install ruff==0.14.10
      - name: Ruff lint
        run: ruff check src/ orchestrator/ tests/
      - name: Ruff format check
        run: ruff format --check src/ orchestrator/ tests/
      - name: Verify Playwright pin consistency
        run: |
          # The agent image has no Playwright; the pin only feeds the
          # workspace/VM builds (.playwright-version is the source of truth,
          # passed as a build-arg). Guard the two fallback copies against drift.
          PIN=$(tr -d '[:space:]' < .playwright-version)
          test -n "$PIN" || { echo "::error::.playwright-version is empty"; exit 1; }
          grep -q "^ARG PLAYWRIGHT_VERSION=${PIN}$" docker/Dockerfile.workspace \
            || { echo "::error::ARG PLAYWRIGHT_VERSION default in docker/Dockerfile.workspace does not match .playwright-version (${PIN})"; exit 1; }
          grep -q "^PLAYWRIGHT_VERSION=${PIN}$" .env.example \
            || { echo "::error::PLAYWRIGHT_VERSION in .env.example does not match .playwright-version (${PIN})"; exit 1; }

  # ---------------------------------------------------------------------------
  # Chart correctness gates (Phase 1) — render matrix + schema validation.
  # Full-power branch: always runs, every step is a hard gate.
  #
  # The installer-* scenarios are the self-host config generator's verbatim
  # output. The generator moved to the private srw-cloud repo on 2026-08-15;
  # these fixtures are the half of the contract that stays here, so a chart
  # change that breaks the installer fails in the repo that caused it rather
  # than days later in srw-cloud's CI. Regenerate them from that repo's
  # www/test/generator.drift.test.mjs CASES if the generator changes.
  # ---------------------------------------------------------------------------
  chart-test:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        scenario:
          - test
          - customer-external
          - eval
          - vm
          - vm-external
          - installer-evaluation
          - installer-production
          - installer-production-vms
    steps:
      - uses: actions/checkout@v4

      - name: Install Helm
        uses: azure/setup-helm@v4
        with:
          version: v3.17.0

      - name: Build Helm dependencies
        run: |
          helm repo add collabora https://collaboraonline.github.io/online --force-update
          helm repo add cloudnative-pg https://cloudnative-pg.github.io/charts --force-update
          helm dependency build helm/

      - name: Install kubeconform
        run: |
          curl -fsSL https://github.com/yannh/kubeconform/releases/download/v0.6.7/kubeconform-linux-amd64.tar.gz \
            | sudo tar -xz -C /usr/local/bin kubeconform
          kubeconform -v

      - name: Helm lint (${{ matrix.scenario }})
        run: helm lint helm/ -f helm/ci/${{ matrix.scenario }}-values.yaml

      - name: Render + kubeconform (${{ matrix.scenario }})
        run: |
          helm template srw helm/ -f helm/ci/${{ matrix.scenario }}-values.yaml \
            | kubeconform \
                -kubernetes-version 1.28.0 \
                -ignore-missing-schemas \
                -schema-location default \
                -schema-location 'https://raw.githubusercontent.com/datreeio/CRDs-catalog/main/{{.Group}}/{{.ResourceKind}}_{{.ResourceAPIVersion}}.json' \
                -summary

      - name: Verify chart-created Secret contract
        if: matrix.scenario == 'eval'
        run: bash scripts/check-chart-created-secret.sh

  # ---------------------------------------------------------------------------
  # Schema negative test — proves values.schema.json rejects bad input. A
  # deliberately-invalid values file MUST make `helm template` fail. Hard gate.
  # ---------------------------------------------------------------------------
  chart-schema-negative:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4

      - name: Install Helm
        uses: azure/setup-helm@v4
        with:
          version: v3.17.0

      - name: Build Helm dependencies
        run: |
          helm repo add collabora https://collaboraonline.github.io/online --force-update
          helm repo add cloudnative-pg https://cloudnative-pg.github.io/charts --force-update
          helm dependency build helm/

      - name: Assert invalid values are rejected by the schema
        run: |
          if helm template srw helm/ -f helm/ci/invalid-values.yaml >/dev/null 2>&1; then
            echo "::error::helm/ci/invalid-values.yaml rendered successfully — values.schema.json is not rejecting it. The schema has stopped validating."
            exit 1
          fi
          echo "OK: invalid-values.yaml was correctly rejected by values.schema.json"

  # ---------------------------------------------------------------------------
  # CodeQL — static analysis for security vulnerabilities
  # NOTE: CodeQL upload requires GitHub Advanced Security on private repos.
  # continue-on-error keeps the pipeline green until the repo is public.
  # ---------------------------------------------------------------------------
  codeql:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    continue-on-error: true
    permissions:
      security-events: write
      contents: read
    strategy:
      fail-fast: false
      matrix:
        language: [python, javascript-typescript]
    steps:
      - uses: actions/checkout@v4
      - uses: github/codeql-action/init@v3
        with:
          languages: ${{ matrix.language }}
      - uses: github/codeql-action/autobuild@v3
      - uses: github/codeql-action/analyze@v3

  # ---------------------------------------------------------------------------
  # Dependency audit
  # ---------------------------------------------------------------------------
  dependency-audit:
    needs: [lint]
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - uses: astral-sh/setup-uv@v6
        with:
          enable-cache: true
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: cockpit/package-lock.json
      - name: Upgrade pip
        # CVE-2026-6357: pip < 26.1 runs self-update import after wheel install
        run: uv pip install --system --upgrade 'pip>=26.1'
      - name: Install pip-audit
        run: uv pip install --system pip-audit
      - name: Install Python dependencies
        run: uv pip install --system -r requirements.txt
      - name: Audit Python dependencies
        # CVE-2026-4539: pygments ReDoS in AdlLexer — local-only, no fix released
        # CVE-2026-25645: requests extract_zipped_paths() temp path — we don't call it;
        #   arxiv pins requests<2.33.0 so we can't upgrade until arxiv releases a fix
        # CVE-2026-3219: pip treats concatenated tar+ZIP files as ZIP — no fix released;
        #   requires a maliciously crafted package, not exposed via our install flow
        # CVE-2026-44405: paramiko rsakey.py allows SHA-1 — no released fix yet (only on
        #   master at commit a448945). Exploitation requires MITM on the SSH channel; our
        #   connections are cluster-internal to controlled workspace pods/VMs.
        #
        # PYSEC-2024-277 (joblib): disputed by upstream; NumpyArrayWrapper is only used
        #   while caching trusted content. No fix released.
        # PYSEC-2024-278 (langchain-community): TFIDFRetriever.load_local SSRF — the
        #   advisory itself says "Upgrading to version 0.0.27 is able to address this"
        #   and we resolve to 0.4.1. OSV metadata lacks a fixed-version field so
        #   pip-audit still flags it.
        # PYSEC-2025-144/145/146/147, PYSEC-2026-101/102 (ollama): every advisory targets
        #   the Ollama Go server (Modelfile parsing, /api/pull, fs/ggml/gguf.go). We use
        #   the `ollama` PyPI package as a thin HTTP client against in-cluster endpoints;
        #   the affected server binary isn't shipped by our images.
        # PYSEC-2025-183 (pyjwt): disputed; the "weak encryption" claim depends on
        #   caller-chosen key length, which is enforced by Keycloak in our deployment.
        # PYSEC-2025-189..197, 210, PYSEC-2026-139, CVE-2025-3000 (torch): all require
        #   loading an attacker-supplied checkpoint or running torch.jit on attacker
        #   code. Agents only pull torch transitively via sentence-transformers against
        #   pinned, vetted embedding models. Most have no fix released.
        # PYSEC-2025-211..218 (transformers): deserialization of untrusted model files
        #   (X-CLIP, GLM4, Perceiver, Transformer-XL, megatron_gpt2, SEW/SEW-D/HuBERT
        #   convert_config). Same threat model — we don't convert or load attacker-
        #   supplied checkpoints, and no fix versions are listed upstream.
        run: >-
          pip-audit --desc
          --ignore-vuln CVE-2026-4539
          --ignore-vuln CVE-2026-25645
          --ignore-vuln CVE-2026-3219
          --ignore-vuln CVE-2026-44405
          --ignore-vuln PYSEC-2024-277
          --ignore-vuln PYSEC-2024-278
          --ignore-vuln PYSEC-2025-144
          --ignore-vuln PYSEC-2025-145
          --ignore-vuln PYSEC-2025-146
          --ignore-vuln PYSEC-2025-147
          --ignore-vuln PYSEC-2026-101
          --ignore-vuln PYSEC-2026-102
          --ignore-vuln PYSEC-2025-183
          --ignore-vuln PYSEC-2025-189
          --ignore-vuln PYSEC-2025-190
          --ignore-vuln PYSEC-2025-191
          --ignore-vuln PYSEC-2025-192
          --ignore-vuln PYSEC-2025-193
          --ignore-vuln PYSEC-2025-194
          --ignore-vuln PYSEC-2025-195
          --ignore-vuln PYSEC-2025-196
          --ignore-vuln PYSEC-2025-197
          --ignore-vuln PYSEC-2025-210
          --ignore-vuln PYSEC-2026-139
          --ignore-vuln CVE-2025-3000
          --ignore-vuln PYSEC-2025-211
          --ignore-vuln PYSEC-2025-212
          --ignore-vuln PYSEC-2025-213
          --ignore-vuln PYSEC-2025-214
          --ignore-vuln PYSEC-2025-215
          --ignore-vuln PYSEC-2025-216
          --ignore-vuln PYSEC-2025-217
          --ignore-vuln PYSEC-2025-218
      - name: Audit npm dependencies
        working-directory: cockpit
        # lodash-es 4.17.23 is the latest release; no fix available.
        # Transitive via mermaid → langium → chevrotain → lodash-es.
        # GHSA-r5fr-rjxr-66jc: Code Injection via _.template (high)
        # GHSA-f23m-r3pf-42rh: Prototype Pollution via _.unset/_.omit (moderate)
        #
        # esbuild 0.27.3 is exact-pinned by @angular/build@21; vite pins ^0.27,
        # so the patched 0.28.1 is unreachable without breaking the Angular 21
        # toolchain. Neither advisory applies to our usage (npm/Linux build, no
        # dev server exposed). Ignoring esbuild's advisories propagates up the
        # via-chain to vite → @vitejs/plugin-basic-ssl → @angular/build.
        # GHSA-gv7w-rqvm-qjhr: RCE via the esbuild Deno module + NPM_CONFIG_REGISTRY
        #   (Deno-only loader — unused here) (high)
        # GHSA-g7r4-m6w7-qqqr: arbitrary file read in the dev server on Windows
        #   (dev-server + Windows only; CI/prod run `ng build` on Linux) (high)
        run: |
          npm audit --json | python3 -c "
          import json, sys
          IGNORE_ADVISORIES = {
              'GHSA-r5fr-rjxr-66jc', 'GHSA-f23m-r3pf-42rh',
              'GHSA-gv7w-rqvm-qjhr', 'GHSA-g7r4-m6w7-qqqr',
          }
          data = json.load(sys.stdin)
          vulns = data.get('vulnerabilities', {})

          ignored_pkgs = set()
          changed = True
          while changed:
              changed = False
              for name, vuln in vulns.items():
                  if name in ignored_pkgs:
                      continue
                  dominated = True
                  for v in vuln.get('via', []):
                      if isinstance(v, dict):
                          ghsa = v.get('url', '').split('/')[-1]
                          if ghsa not in IGNORE_ADVISORIES:
                              dominated = False
                              break
                      elif isinstance(v, str) and v not in ignored_pkgs:
                          dominated = False
                          break
                  if dominated:
                      ignored_pkgs.add(name)
                      changed = True

          found = 0
          for name, vuln in vulns.items():
              if name in ignored_pkgs:
                  continue
              if vuln.get('severity') in ('high', 'critical'):
                  found += 1
                  print(f'{name}: {vuln[\"severity\"]}')
          if found:
              print(f'{found} unignored high/critical vulnerabilities')
              sys.exit(1)
          print('npm audit passed (ignored advisories: ' + ', '.join(IGNORE_ADVISORIES) + ')')
          "

      # --- Third-party license policy gate (blocking) -----------------------
      # Fails the release if a bundled dependency carries a denied license
      # (GPL/AGPL/SSPL/BSL/...). Server images the chart pulls (Neo4j/Postgres/
      # Mongo) are out of scope — we reference, not convey them — but their
      # bundled *client drivers* (neo4j/psycopg/pymongo) ARE covered. Lives in
      # this already-required job so it gates every build without per-job edits.
      # The committed THIRD_PARTY_LICENSES.md is regenerated upstream on develop.
      # Source of truth: scripts/check_licenses.py.
      - name: Install license tooling + full bundled dep set
        run: >-
          uv pip install --system pip-licenses
          -r orchestrator/requirements.txt
          -r orchestrator/mcp/requirements.txt
      - name: Check third-party licenses
        # Python: installed metadata via pip-licenses. Frontend: read straight
        # from cockpit/package-lock.json (no npm install / node_modules needed).
        run: python scripts/check_licenses.py --check

  # ---------------------------------------------------------------------------
  # Python tests — always run on main (not change-based)
  # ---------------------------------------------------------------------------
  test-python:
    needs: [lint]
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - uses: astral-sh/setup-uv@v6
        with:
          enable-cache: true
      # The chart render contract tests in tests/test_canvas_slice3_infra.py
      # shell out to `helm template`. ubuntu-latest ships Helm, so their
      # `which("helm")` skip guard never fires here; without the subchart
      # vendored they fail on the Collabora dependency instead of skipping.
      - name: Install Helm
        uses: azure/setup-helm@v4
        with:
          version: v3.17.0
      - name: Build Helm dependencies
        run: |
          helm repo add collabora https://collaboraonline.github.io/online --force-update
          helm repo add cloudnative-pg https://cloudnative-pg.github.io/charts --force-update
          helm dependency build helm/
      - name: Install dependencies
        run: uv pip install --system -r requirements.txt -r orchestrator/requirements.txt -r orchestrator/mcp/requirements.txt -r requirements-dev.txt pytest pytest-asyncio
      # Keep tests from the same file on one worker so file-local fixture/order
      # assumptions remain intact. The runner caps worker count and also scales
      # down automatically on smaller GitHub-hosted runners.
      - name: Run tests
        run: ./scripts/pytest-fast.sh

  # ---------------------------------------------------------------------------
  # Cockpit unit + production-browser conformance tests — always run on main
  # ---------------------------------------------------------------------------
  test-cockpit:
    needs: [lint]
    runs-on: ubuntu-latest
    # The engines and every system library they need are baked into this image
    # at /ms-playwright. The step this replaces (`playwright install --with-deps
    # chromium firefox webkit`) pulled 181 apt packages / 114 MB from
    # azure.archive.ubuntu.com on every run. That normally cost ~35 s, but a
    # degraded mirror on 2026-08-18 stretched it to 18m40s and killed the job on
    # its wall clock. On main that blocks resolve-version and every image build,
    # so keep the engines off apt entirely.
    # PLAYWRIGHT_VERSION must match this tag; the guard step below enforces it.
    container:
      image: mcr.microsoft.com/playwright:v1.59.0-noble
    env:
      PLAYWRIGHT_VERSION: 1.59.0
      # A container job runs as root with HOME=/github/home, a runner-owned
      # bind mount (pwuser inside this image). Firefox refuses to launch when
      # $HOME belongs to another user ("Running Nightly as root in a regular
      # user's session is not supported"), so all 11 firefox specs died at
      # browser launch on the first cockpit push after the image switch, while
      # chromium and webkit passed. /root is owned by the user this job
      # actually runs as. This is Playwright's documented workaround; drop it
      # only if the job stops running as root.
      HOME: /root
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      # The image ships Node 24; pin to 22 so unit tests, the production build,
      # and the conformance run all use the same Node as every other job.
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: cockpit/package-lock.json
      - name: Install dependencies
        working-directory: cockpit
        run: npm ci
      # Engines come from the image, so a @playwright/test bump that forgets the
      # container tag would otherwise surface minutes later as a confusing
      # "Executable doesn't exist". Bump the tag and PLAYWRIGHT_VERSION together.
      - name: Verify Playwright matches the container image
        working-directory: cockpit
        run: |
          HAVE=$(node -p "require('@playwright/test/package.json').version")
          if [ "$HAVE" != "$PLAYWRIGHT_VERSION" ]; then
            echo "::error::@playwright/test is $HAVE but this job runs mcr.microsoft.com/playwright:v$PLAYWRIGHT_VERSION-noble."
            echo "::error::Bump the container image tag and PLAYWRIGHT_VERSION in this workflow to v$HAVE-noble."
            exit 1
          fi
          echo "Playwright $HAVE matches the container image."
      - name: Check i18n parity
        working-directory: cockpit
        run: npm run i18n:check
      # Step-level caps so a hung step names itself instead of silently eating
      # the job budget. Healthy times are ~70 s / ~25 s / ~75 s.
      - name: Run tests
        working-directory: cockpit
        timeout-minutes: 10
        run: npm test
      - name: Build production Cockpit
        working-directory: cockpit
        timeout-minutes: 10
        run: npm run build
      - name: Run Dynamic Canvas browser conformance
        working-directory: cockpit
        timeout-minutes: 10
        run: npm run test:e2e:canvas:no-build
      - name: Upload Dynamic Canvas browser report
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: canvas-browser-report-main-${{ github.run_attempt }}
          path: |
            cockpit/playwright-report/
            cockpit/test-results/
          if-no-files-found: ignore
          retention-days: 7

  # ---------------------------------------------------------------------------
  # Auto patch-bump version from last git v-tag
  # ---------------------------------------------------------------------------
  resolve-version:
    needs: [lint, dependency-audit, test-python, test-cockpit]
    if: >-
      always() && !cancelled() &&
      github.event_name == 'push' &&
      needs.lint.result == 'success' &&
      needs.dependency-audit.result == 'success' &&
      needs.test-python.result == 'success' &&
      needs.test-cockpit.result == 'success'
    runs-on: ubuntu-latest
    timeout-minutes: 5
    outputs:
      version: ${{ steps.bump.outputs.version }}
      tag: ${{ steps.bump.outputs.tag }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Compute next patch version
        id: bump
        run: |
          git fetch --tags --force
          LAST=$(git tag -l 'v*' --sort=-v:refname | head -n1)
          if [ -z "$LAST" ]; then
            LAST="v0.0.0"
          fi
          MAJOR=$(echo "${LAST#v}" | cut -d. -f1)
          MINOR=$(echo "${LAST#v}" | cut -d. -f2)
          PATCH=$(echo "${LAST#v}" | cut -d. -f3)
          NEW="${MAJOR}.${MINOR}.$((PATCH + 1))"
          echo "version=$NEW" >> "$GITHUB_OUTPUT"
          echo "tag=v$NEW" >> "$GITHUB_OUTPUT"
          echo "Last tag: $LAST -> next: v$NEW"

  # ---------------------------------------------------------------------------
  # Image builds — run on main pushes AND on PRs to main.
  # On push: build + push (sha + latest + version tags), full release flow.
  # On PR: build only — no docker login, no version tag, no registry push.
  # The Packer / Dockerfile invocations themselves still execute, which is
  # the whole point: we catch broken builds before merge instead of after.
  # All require lint + dep-audit + tests to pass.
  # ---------------------------------------------------------------------------
  build-agent:
    needs: [lint, dependency-audit, test-python, test-cockpit, resolve-version]
    if: >-
      always() && !cancelled() &&
      needs.lint.result == 'success' &&
      needs.dependency-audit.result == 'success' &&
      needs.test-python.result == 'success' &&
      needs.test-cockpit.result == 'success' &&
      (needs.resolve-version.result == 'success' || needs.resolve-version.result == 'skipped')
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4
      - name: Free disk space
        # Hosted runners have ~14 GiB free and these jobs need more. Self-hosted
        # runners do not need it, and the heavy variants below delete
        # /opt/hostedtoolcache, which is where setup-* install. Guarded rather
        # than deleted so the hosted fallback (gh variable delete CI_RUNNER_LABEL)
        # still works.
        if: runner.environment == 'github-hosted'
        run: |
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc
      - uses: docker/setup-buildx-action@v3
        with:
          buildkitd-config-inline: |
            [registry."docker.io"]
              mirrors = ["mirror.gcr.io"]
      - uses: docker/login-action@v3
        if: github.event_name != 'pull_request'
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}-agent
          tags: |
            type=sha,format=short,prefix=sha-
            type=raw,value=latest,enable=${{ github.event_name != 'pull_request' }}
            type=raw,value=${{ needs.resolve-version.outputs.tag }},enable=${{ github.event_name != 'pull_request' }}
      - id: short_sha
        run: echo "sha=${GITHUB_SHA::7}" >> "$GITHUB_OUTPUT"
      - name: Build${{ github.event_name == 'pull_request' && ' (PR validation, no push)' || ' and push' }}
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./docker/Dockerfile.agent
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: |
            ${{ steps.meta.outputs.labels }}
            org.opencontainers.image.source=${{ env.SRW_SOURCE_URL }}
            org.opencontainers.image.revision=${{ github.sha }}
            org.opencontainers.image.version=${{ needs.resolve-version.outputs.version }}
            org.opencontainers.image.documentation=${{ env.SRW_DOCUMENTATION_URL }}
            io.srw.component=agent
          provenance: false
          cache-from: type=registry,ref=${{ env.REGISTRY }}/${{ env.REPO_LOWER }}-agent:buildcache-main
          cache-to: type=registry,ref=${{ env.REGISTRY }}/${{ env.REPO_LOWER }}-agent:buildcache-main,mode=max,image-manifest=true,oci-mediatypes=true,compression=zstd,ignore-error=true
          build-args: |
            BUILD_SHA=${{ steps.short_sha.outputs.sha }}
            SRW_SOURCE_REVISION=${{ github.sha }}
            SRW_SOURCE_URL=${{ env.SRW_SOURCE_URL }}
            SRW_RELEASE_VERSION=${{ needs.resolve-version.outputs.version }}
            SRW_DOCUMENTATION_URL=${{ env.SRW_DOCUMENTATION_URL }}

  build-orchestrator:
    needs: [lint, dependency-audit, test-python, test-cockpit, resolve-version]
    if: >-
      always() && !cancelled() &&
      needs.lint.result == 'success' &&
      needs.dependency-audit.result == 'success' &&
      needs.test-python.result == 'success' &&
      needs.test-cockpit.result == 'success' &&
      (needs.resolve-version.result == 'success' || needs.resolve-version.result == 'skipped')
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
        with:
          buildkitd-config-inline: |
            [registry."docker.io"]
              mirrors = ["mirror.gcr.io"]
      - uses: docker/login-action@v3
        if: github.event_name != 'pull_request'
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}-orchestrator
          tags: |
            type=sha,format=short,prefix=sha-
            type=raw,value=latest,enable=${{ github.event_name != 'pull_request' }}
            type=raw,value=${{ needs.resolve-version.outputs.tag }},enable=${{ github.event_name != 'pull_request' }}
      - name: Build${{ github.event_name == 'pull_request' && ' (PR validation, no push)' || ' and push' }}
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./docker/Dockerfile.orchestrator
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: |
            ${{ steps.meta.outputs.labels }}
            org.opencontainers.image.source=${{ env.SRW_SOURCE_URL }}
            org.opencontainers.image.revision=${{ github.sha }}
            org.opencontainers.image.version=${{ needs.resolve-version.outputs.version }}
            org.opencontainers.image.documentation=${{ env.SRW_DOCUMENTATION_URL }}
            io.srw.component=orchestrator
          provenance: false
          build-args: |
            SRW_SOURCE_REVISION=${{ github.sha }}
            SRW_SOURCE_URL=${{ env.SRW_SOURCE_URL }}
            SRW_RELEASE_VERSION=${{ needs.resolve-version.outputs.version }}
            SRW_DOCUMENTATION_URL=${{ env.SRW_DOCUMENTATION_URL }}
          cache-from: type=registry,ref=${{ env.REGISTRY }}/${{ env.REPO_LOWER }}-orchestrator:buildcache-main
          cache-to: type=registry,ref=${{ env.REGISTRY }}/${{ env.REPO_LOWER }}-orchestrator:buildcache-main,mode=max,image-manifest=true,oci-mediatypes=true,compression=zstd,ignore-error=true

  build-cockpit:
    needs: [lint, dependency-audit, test-python, test-cockpit, resolve-version]
    if: >-
      always() && !cancelled() &&
      needs.lint.result == 'success' &&
      needs.dependency-audit.result == 'success' &&
      needs.test-python.result == 'success' &&
      needs.test-cockpit.result == 'success' &&
      (needs.resolve-version.result == 'success' || needs.resolve-version.result == 'skipped')
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
        with:
          buildkitd-config-inline: |
            [registry."docker.io"]
              mirrors = ["mirror.gcr.io"]
      - uses: docker/login-action@v3
        if: github.event_name != 'pull_request'
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}-cockpit
          tags: |
            type=sha,format=short,prefix=sha-
            type=raw,value=latest,enable=${{ github.event_name != 'pull_request' }}
            type=raw,value=${{ needs.resolve-version.outputs.tag }},enable=${{ github.event_name != 'pull_request' }}
      - name: Build${{ github.event_name == 'pull_request' && ' (PR validation, no push)' || ' and push' }}
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./docker/Dockerfile.cockpit
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: |
            ${{ steps.meta.outputs.labels }}
            org.opencontainers.image.source=${{ env.SRW_SOURCE_URL }}
            org.opencontainers.image.revision=${{ github.sha }}
            org.opencontainers.image.version=${{ needs.resolve-version.outputs.version }}
            org.opencontainers.image.documentation=${{ env.SRW_DOCUMENTATION_URL }}
            io.srw.component=cockpit
          provenance: false
          build-args: |
            SRW_SOURCE_REVISION=${{ github.sha }}
            SRW_SOURCE_URL=${{ env.SRW_SOURCE_URL }}
            SRW_RELEASE_VERSION=${{ needs.resolve-version.outputs.version }}
            SRW_DOCUMENTATION_URL=${{ env.SRW_DOCUMENTATION_URL }}
          cache-from: type=registry,ref=${{ env.REGISTRY }}/${{ env.REPO_LOWER }}-cockpit:buildcache-main
          cache-to: type=registry,ref=${{ env.REGISTRY }}/${{ env.REPO_LOWER }}-cockpit:buildcache-main,mode=max,image-manifest=true,oci-mediatypes=true,compression=zstd,ignore-error=true

  build-mcp:
    needs: [lint, dependency-audit, test-python, test-cockpit, resolve-version]
    if: >-
      always() && !cancelled() &&
      needs.lint.result == 'success' &&
      needs.dependency-audit.result == 'success' &&
      needs.test-python.result == 'success' &&
      needs.test-cockpit.result == 'success' &&
      (needs.resolve-version.result == 'success' || needs.resolve-version.result == 'skipped')
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
        with:
          buildkitd-config-inline: |
            [registry."docker.io"]
              mirrors = ["mirror.gcr.io"]
      - uses: docker/login-action@v3
        if: github.event_name != 'pull_request'
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}-mcp
          tags: |
            type=sha,format=short,prefix=sha-
            type=raw,value=latest,enable=${{ github.event_name != 'pull_request' }}
            type=raw,value=${{ needs.resolve-version.outputs.tag }},enable=${{ github.event_name != 'pull_request' }}
      - name: Build${{ github.event_name == 'pull_request' && ' (PR validation, no push)' || ' and push' }}
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./docker/Dockerfile.mcp
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: |
            ${{ steps.meta.outputs.labels }}
            org.opencontainers.image.source=${{ env.SRW_SOURCE_URL }}
            org.opencontainers.image.revision=${{ github.sha }}
            org.opencontainers.image.version=${{ needs.resolve-version.outputs.version }}
            org.opencontainers.image.documentation=${{ env.SRW_DOCUMENTATION_URL }}
            io.srw.component=mcp
          provenance: false
          build-args: |
            SRW_SOURCE_REVISION=${{ github.sha }}
            SRW_SOURCE_URL=${{ env.SRW_SOURCE_URL }}
            SRW_RELEASE_VERSION=${{ needs.resolve-version.outputs.version }}
            SRW_DOCUMENTATION_URL=${{ env.SRW_DOCUMENTATION_URL }}
          cache-from: type=registry,ref=${{ env.REGISTRY }}/${{ env.REPO_LOWER }}-mcp:buildcache-main
          cache-to: type=registry,ref=${{ env.REGISTRY }}/${{ env.REPO_LOWER }}-mcp:buildcache-main,mode=max,image-manifest=true,oci-mediatypes=true,compression=zstd,ignore-error=true

  build-workspace:
    needs: [lint, dependency-audit, test-python, test-cockpit, resolve-version]
    if: >-
      always() && !cancelled() &&
      needs.lint.result == 'success' &&
      needs.dependency-audit.result == 'success' &&
      needs.test-python.result == 'success' &&
      needs.test-cockpit.result == 'success' &&
      (needs.resolve-version.result == 'success' || needs.resolve-version.result == 'skipped')
    runs-on: ubuntu-latest
    timeout-minutes: 40
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
        with:
          buildkitd-config-inline: |
            [registry."docker.io"]
              mirrors = ["mirror.gcr.io"]
      - uses: docker/login-action@v3
        if: github.event_name != 'pull_request'
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}-workspace
          tags: |
            type=sha,format=short,prefix=sha-
            type=raw,value=latest,enable=${{ github.event_name != 'pull_request' }}
            type=raw,value=${{ needs.resolve-version.outputs.tag }},enable=${{ github.event_name != 'pull_request' }}
      - name: Read Playwright version
        id: pw
        run: echo "version=$(tr -d '[:space:]' < .playwright-version)" >> "$GITHUB_OUTPUT"
      - name: Build${{ github.event_name == 'pull_request' && ' (PR validation, no push)' || ' and push' }}
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./docker/Dockerfile.workspace
          build-args: |
            PLAYWRIGHT_VERSION=${{ steps.pw.outputs.version }}
            SRW_SOURCE_REVISION=${{ github.sha }}
            SRW_SOURCE_URL=${{ env.SRW_SOURCE_URL }}
            SRW_RELEASE_VERSION=${{ needs.resolve-version.outputs.version }}
            SRW_DOCUMENTATION_URL=${{ env.SRW_DOCUMENTATION_URL }}
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: |
            ${{ steps.meta.outputs.labels }}
            org.opencontainers.image.source=${{ env.SRW_SOURCE_URL }}
            org.opencontainers.image.revision=${{ github.sha }}
            org.opencontainers.image.version=${{ needs.resolve-version.outputs.version }}
            org.opencontainers.image.documentation=${{ env.SRW_DOCUMENTATION_URL }}
            io.srw.component=workspace
          provenance: false
          cache-from: type=registry,ref=${{ env.REGISTRY }}/${{ env.REPO_LOWER }}-workspace:buildcache-main
          cache-to: type=registry,ref=${{ env.REGISTRY }}/${{ env.REPO_LOWER }}-workspace:buildcache-main,mode=max,image-manifest=true,oci-mediatypes=true,compression=zstd,ignore-error=true

  build-sudo-gate:
    needs: [lint, dependency-audit, test-python, test-cockpit, resolve-version]
    if: >-
      always() && !cancelled() &&
      needs.lint.result == 'success' &&
      needs.dependency-audit.result == 'success' &&
      needs.test-python.result == 'success' &&
      needs.test-cockpit.result == 'success' &&
      (needs.resolve-version.result == 'success' || needs.resolve-version.result == 'skipped')
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.22'
      - name: Build sudo-gated (Go daemon)
        working-directory: vm/sudo-daemon
        run: |
          go mod tidy
          CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build \
            -ldflags "-s -w -X main.version=sha-${GITHUB_SHA::7}" \
            -o sudo-gated ./cmd/sudo-gated/
          file sudo-gated
          ls -lh sudo-gated
      - name: Build sudo_gate.so (C plugin)
        working-directory: vm/sudo-plugin
        run: |
          gcc -fPIC -shared -Wall -Wextra -Werror -O2 -I./include \
            -o sudo_gate.so sudo_gate.c json_util.c
          file sudo_gate.so
          ls -lh sudo_gate.so
      - name: Upload sudo gate artifacts
        uses: actions/upload-artifact@v4
        with:
          name: sudo-gate-binaries
          path: |
            vm/sudo-daemon/sudo-gated
            vm/sudo-plugin/sudo_gate.so
          retention-days: 7

  # ---------------------------------------------------------------------------
  # Stage 1: heavy, stable provisioning. On main, runs on push only — PR
  # validation re-uses the existing GHCR :latest stage1 image, which makes PRs
  # to main much faster (only stage2 rebuilds). Cron refresh runs in
  # stage1-rebuild.yml so the baseline never drifts more than a week.
  # ---------------------------------------------------------------------------
  build-agent-vm-base-stage1:
    needs: [lint, dependency-audit, test-python, test-cockpit, resolve-version]
    if: >-
      always() && !cancelled() &&
      github.event_name == 'push' &&
      needs.lint.result == 'success' &&
      needs.dependency-audit.result == 'success' &&
      needs.test-python.result == 'success' &&
      needs.test-cockpit.result == 'success' &&
      needs.resolve-version.result == 'success'
    runs-on: ubuntu-latest
    timeout-minutes: 90
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4
      - name: Free disk space
        # Hosted runners have ~14 GiB free and these jobs need more. Self-hosted
        # runners do not need it, and the heavy variants below delete
        # /opt/hostedtoolcache, which is where setup-* install. Guarded rather
        # than deleted so the hosted fallback (gh variable delete CI_RUNNER_LABEL)
        # still works.
        if: runner.environment == 'github-hosted'
        run: |
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc \
            /usr/local/share/boost /usr/local/share/chromium \
            /usr/share/swift /usr/local/julia* /opt/hostedtoolcache
      - name: Install QEMU and dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y qemu-system-x86 qemu-utils genisoimage
      - name: Enable KVM (hosted runner udev rule)
        # Hosted runners ship /dev/kvm as root:kvm with no world access, so this
        # udev rule is how the runner user reaches it. Inside a container there is
        # no udevd: the rule file goes nowhere and `udevadm control` fails. There,
        # access comes from the pod spec instead — privileged + a /dev/kvm
        # CharDevice hostPath, see HomeLab deployments_managed/arc-runners-kvm/.
        # (Measured on node4: /dev/kvm is gid 36, mode 0666.)
        if: runner.environment == 'github-hosted'
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' | sudo tee /etc/udev/rules.d/99-kvm.rules
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm

      - name: Verify KVM is usable
        # Runs on BOTH runner types, unconditionally. stage1.pkr.hcl and
        # stage2.pkr.hcl set accelerator = "kvm", and without acceleration packer
        # does not fail — it silently falls back to TCG and burns the entire
        # 75-90 minute timeout. Fail in five seconds instead.
        #
        # KVM_GET_API_VERSION = _IO(KVMIO=0xAE, 0) = 0xAE00. Opening the device
        # and getting an answer proves the permissions AND that the host kernel's
        # KVM subsystem is reachable from in here, which `ls` alone does not.
        run: |
          set -euo pipefail
          ls -la /dev/kvm
          id
          python3 -c "import fcntl,os;fd=os.open('/dev/kvm',os.O_RDWR);print('KVM_GET_API_VERSION =',fcntl.ioctl(fd,0xAE00,0))"
      - uses: hashicorp/setup-packer@main
        with:
          version: latest
      - name: Compute cloud image cache key
        id: img-key
        run: echo "key=ubuntu-noble-cloudimg-$(date -u +%Y-%m)" >> "$GITHUB_OUTPUT"
      - name: Cache Ubuntu cloud image
        id: cloud-image-cache
        uses: actions/cache@v4
        with:
          path: docker/agent-vm-base/input/noble-server-cloudimg-amd64.img
          key: ${{ steps.img-key.outputs.key }}
      - name: Download Ubuntu cloud image
        if: steps.cloud-image-cache.outputs.cache-hit != 'true'
        working-directory: docker/agent-vm-base
        run: |
          mkdir -p input
          wget -q https://cloud-images.ubuntu.com/noble/current/noble-server-cloudimg-amd64.img \
            -O input/noble-server-cloudimg-amd64.img
      - name: Verify Ubuntu cloud image
        working-directory: docker/agent-vm-base
        run: qemu-img info input/noble-server-cloudimg-amd64.img
      - name: Packer init (stage1)
        working-directory: docker/agent-vm-base
        run: packer init stage1.pkr.hcl
      - name: Packer build (stage1)
        working-directory: docker/agent-vm-base
        run: |
          PW_VERSION=$(tr -d '[:space:]' < ../../.playwright-version)
          test -n "$PW_VERSION" || { echo "::error::.playwright-version is empty"; exit 1; }
          for attempt in 1 2; do
            if packer build -var "playwright_version=${PW_VERSION}" stage1.pkr.hcl; then
              exit 0
            fi
            echo "::warning::Stage1 packer build attempt ${attempt} failed"
            rm -rf output-stage1
          done
          echo "::error::Stage1 packer build failed after 2 attempts"
          exit 1
      - uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Build and push stage1 containerDisk
        working-directory: docker/agent-vm-base
        run: |
          IMAGE=$(echo "${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}-agent-vm-base-stage1" | tr '[:upper:]' '[:lower:]')
          TAGS=("${IMAGE}:sha-${GITHUB_SHA::7}" "${IMAGE}:latest")
          BUILD_ARGS=(
            --build-arg "SRW_SOURCE_REVISION=${GITHUB_SHA}"
            --build-arg "SRW_SOURCE_URL=${{ env.SRW_SOURCE_URL }}"
            --build-arg "SRW_RELEASE_VERSION=${{ needs.resolve-version.outputs.version }}"
            --build-arg "SRW_DOCUMENTATION_URL=${{ env.SRW_DOCUMENTATION_URL }}"
          )
          for T in "${TAGS[@]}"; do BUILD_ARGS+=(-t "$T"); done
          docker build "${BUILD_ARGS[@]}" -f Dockerfile.containerDisk-stage1 .
          for T in "${TAGS[@]}"; do docker push "$T"; done
          printf 'Pushed stage1:\n'; printf '  %s\n' "${TAGS[@]}"

  # ---------------------------------------------------------------------------
  # Stage 2: light per-commit layer. Pulls the stage1 containerDisk from GHCR
  # (skipped on PRs — uses existing :latest), extracts the qcow2, and layers
  # stage2 on top via stage2.pkr.hcl. ~5-10 min wall-clock.
  # ---------------------------------------------------------------------------
  build-agent-vm-base:
    needs: [lint, dependency-audit, test-python, test-cockpit, build-sudo-gate, resolve-version, build-agent-vm-base-stage1]
    if: >-
      always() && !cancelled() &&
      needs.lint.result == 'success' &&
      needs.dependency-audit.result == 'success' &&
      needs.test-python.result == 'success' &&
      needs.test-cockpit.result == 'success' &&
      needs.build-sudo-gate.result == 'success' &&
      (needs.resolve-version.result == 'success' || needs.resolve-version.result == 'skipped') &&
      (needs.build-agent-vm-base-stage1.result == 'success' || needs.build-agent-vm-base-stage1.result == 'skipped')
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: read
      packages: write
    steps:
      - name: Fail if sudo-gate build failed
        if: needs.build-sudo-gate.result == 'failure'
        run: |
          echo "::error::build-sudo-gate failed — refusing to build VM image without gate binaries"
          exit 1
      - uses: actions/checkout@v4
      - name: Download sudo gate artifacts
        uses: actions/download-artifact@v4
        with:
          name: sudo-gate-binaries
          path: docker/agent-vm-base/files/
        continue-on-error: true
      - name: Prepare sudo gate binaries for Packer
        working-directory: docker/agent-vm-base
        # upload-artifact@v4 roots the archive at the LONGEST COMMON ANCESTOR
        # of its paths (vm/), so the artifact extracts as sudo-daemon/... and
        # sudo-plugin/... — no vm/ prefix. This step checked files/vm/... and
        # silently shipped empty placeholders (VM images had no sudo gate).
        run: |
          if [ -f files/sudo-daemon/sudo-gated ]; then
            mv files/sudo-daemon/sudo-gated files/sudo-gated-bin
            echo "sudo-gated binary ready ($(wc -c < files/sudo-gated-bin) bytes)"
          else
            touch files/sudo-gated-bin
            echo "sudo-gated binary not available — creating empty placeholder"
          fi
          if [ -f files/sudo-plugin/sudo_gate.so ]; then
            mv files/sudo-plugin/sudo_gate.so files/sudo_gate.so
            echo "sudo_gate.so ready ($(wc -c < files/sudo_gate.so) bytes)"
          else
            touch files/sudo_gate.so
            echo "sudo_gate.so not available — creating empty placeholder"
          fi
          ls -la files/
      - name: Free disk space
        # Hosted runners have ~14 GiB free and these jobs need more. Self-hosted
        # runners do not need it, and the heavy variants below delete
        # /opt/hostedtoolcache, which is where setup-* install. Guarded rather
        # than deleted so the hosted fallback (gh variable delete CI_RUNNER_LABEL)
        # still works.
        if: runner.environment == 'github-hosted'
        run: |
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc \
            /usr/local/share/boost /usr/local/share/chromium \
            /usr/share/swift /usr/local/julia* /opt/hostedtoolcache
      - name: Install QEMU and dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y qemu-system-x86 qemu-utils genisoimage
      - name: Enable KVM (hosted runner udev rule)
        # Hosted runners ship /dev/kvm as root:kvm with no world access, so this
        # udev rule is how the runner user reaches it. Inside a container there is
        # no udevd: the rule file goes nowhere and `udevadm control` fails. There,
        # access comes from the pod spec instead — privileged + a /dev/kvm
        # CharDevice hostPath, see HomeLab deployments_managed/arc-runners-kvm/.
        # (Measured on node4: /dev/kvm is gid 36, mode 0666.)
        if: runner.environment == 'github-hosted'
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' | sudo tee /etc/udev/rules.d/99-kvm.rules
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm

      - name: Verify KVM is usable
        # Runs on BOTH runner types, unconditionally. stage1.pkr.hcl and
        # stage2.pkr.hcl set accelerator = "kvm", and without acceleration packer
        # does not fail — it silently falls back to TCG and burns the entire
        # 75-90 minute timeout. Fail in five seconds instead.
        #
        # KVM_GET_API_VERSION = _IO(KVMIO=0xAE, 0) = 0xAE00. Opening the device
        # and getting an answer proves the permissions AND that the host kernel's
        # KVM subsystem is reachable from in here, which `ls` alone does not.
        run: |
          set -euo pipefail
          ls -la /dev/kvm
          id
          python3 -c "import fcntl,os;fd=os.open('/dev/kvm',os.O_RDWR);print('KVM_GET_API_VERSION =',fcntl.ioctl(fd,0xAE00,0))"
      - uses: hashicorp/setup-packer@main
        with:
          version: latest
      # Login is unconditional here — needed to PULL the stage1 containerDisk
      # from GHCR even on PR validation runs. Push of the final image still
      # gates on push events below.
      - uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Pull stage1 containerDisk and extract qcow2
        working-directory: docker/agent-vm-base
        run: |
          STAGE1_IMAGE=$(echo "${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}-agent-vm-base-stage1:latest" | tr '[:upper:]' '[:lower:]')
          docker pull "$STAGE1_IMAGE"
          # FROM scratch images have no shell so a real CMD doesn't matter — we
          # never start the container, just create-then-cp.
          CID=$(docker create "$STAGE1_IMAGE" /noop 2>/dev/null || docker create "$STAGE1_IMAGE")
          mkdir -p input
          docker cp "$CID":/disk/agent-vm-base-stage1.qcow2 input/agent-vm-base-stage1.qcow2
          docker rm "$CID"
          qemu-img info input/agent-vm-base-stage1.qcow2
      - name: Packer init (stage2)
        working-directory: docker/agent-vm-base
        run: packer init stage2.pkr.hcl
      - name: Packer build (stage2)
        working-directory: docker/agent-vm-base
        run: |
          for attempt in 1 2; do
            if packer build stage2.pkr.hcl; then
              exit 0
            fi
            echo "::warning::Stage2 packer build attempt ${attempt} failed"
            rm -rf output
          done
          echo "::error::Stage2 packer build failed after 2 attempts"
          exit 1
      - name: Build${{ github.event_name == 'pull_request' && ' (PR validation, no push)' || ' and push' }} containerDisk image
        working-directory: docker/agent-vm-base
        run: |
          IMAGE=$(echo "${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}-agent-vm-base" | tr '[:upper:]' '[:lower:]')
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            # PR validation: build with a single throwaway tag, no push.
            TAGS=("${IMAGE}:pr-validate-${GITHUB_SHA::7}")
          else
            TAGS=("${IMAGE}:sha-${GITHUB_SHA::7}" "${IMAGE}:latest" "${IMAGE}:${{ needs.resolve-version.outputs.tag }}")
          fi
          BUILD_ARGS=(
            --build-arg "SRW_SOURCE_REVISION=${GITHUB_SHA}"
            --build-arg "SRW_SOURCE_URL=${{ env.SRW_SOURCE_URL }}"
            --build-arg "SRW_RELEASE_VERSION=${{ needs.resolve-version.outputs.version }}"
            --build-arg "SRW_DOCUMENTATION_URL=${{ env.SRW_DOCUMENTATION_URL }}"
          )
          for T in "${TAGS[@]}"; do BUILD_ARGS+=(-t "$T"); done
          docker build "${BUILD_ARGS[@]}" -f Dockerfile.containerDisk .
          if [ "${{ github.event_name }}" != "pull_request" ]; then
            for T in "${TAGS[@]}"; do docker push "$T"; done
            printf 'Pushed:\n'; printf '  %s\n' "${TAGS[@]}"
          else
            printf 'Built (PR validation, not pushed):\n'; printf '  %s\n' "${TAGS[@]}"
          fi

  build-vm-controller:
    needs: [lint, dependency-audit, test-python, test-cockpit, resolve-version]
    if: >-
      always() && !cancelled() &&
      needs.lint.result == 'success' &&
      needs.dependency-audit.result == 'success' &&
      needs.test-python.result == 'success' &&
      needs.test-cockpit.result == 'success' &&
      (needs.resolve-version.result == 'success' || needs.resolve-version.result == 'skipped')
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
        with:
          buildkitd-config-inline: |
            [registry."docker.io"]
              mirrors = ["mirror.gcr.io"]
      - uses: docker/login-action@v3
        if: github.event_name != 'pull_request'
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}-vm-controller
          tags: |
            type=sha,format=short,prefix=sha-
            type=raw,value=latest,enable=${{ github.event_name != 'pull_request' }}
            type=raw,value=${{ needs.resolve-version.outputs.tag }},enable=${{ github.event_name != 'pull_request' }}
      - name: Build${{ github.event_name == 'pull_request' && ' (PR validation, no push)' || ' and push' }}
        uses: docker/build-push-action@v5
        with:
          context: ./vm/controller
          file: ./vm/controller/Dockerfile
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: |
            ${{ steps.meta.outputs.labels }}
            org.opencontainers.image.source=${{ env.SRW_SOURCE_URL }}
            org.opencontainers.image.revision=${{ github.sha }}
            org.opencontainers.image.version=${{ needs.resolve-version.outputs.version }}
            org.opencontainers.image.documentation=${{ env.SRW_DOCUMENTATION_URL }}
            io.srw.component=vm-controller
          provenance: false
          build-args: |
            SRW_SOURCE_REVISION=${{ github.sha }}
            SRW_SOURCE_URL=${{ env.SRW_SOURCE_URL }}
            SRW_RELEASE_VERSION=${{ needs.resolve-version.outputs.version }}
            SRW_DOCUMENTATION_URL=${{ env.SRW_DOCUMENTATION_URL }}
          cache-from: type=registry,ref=${{ env.REGISTRY }}/${{ env.REPO_LOWER }}-vm-controller:buildcache-main
          cache-to: type=registry,ref=${{ env.REGISTRY }}/${{ env.REPO_LOWER }}-vm-controller:buildcache-main,mode=max,image-manifest=true,oci-mediatypes=true,compression=zstd,ignore-error=true

  # ---------------------------------------------------------------------------
  # Package and publish Helm chart at the auto-bumped version.
  # Git tag goes LAST so a failed chart push doesn't leave dangling tags.
  # ---------------------------------------------------------------------------
  release-chart:
    name: Package and publish Helm chart
    needs: [resolve-version, build-agent, build-orchestrator, build-cockpit, build-mcp, build-workspace, build-vm-controller, build-agent-vm-base, chart-test]
    if: >-
      always() && !cancelled() &&
      github.event_name == 'push' &&
      needs.resolve-version.result == 'success' &&
      needs.build-agent.result == 'success' &&
      needs.build-orchestrator.result == 'success' &&
      needs.build-cockpit.result == 'success' &&
      needs.build-mcp.result == 'success' &&
      needs.build-agent-vm-base.result == 'success' &&
      !contains(needs.build-workspace.result, 'failure') &&
      needs.chart-test.result == 'success'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: write
      packages: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Install Helm
        uses: azure/setup-helm@v4
        with:
          version: v3.17.0
      - name: Install yq
        run: |
          sudo wget -qO /usr/local/bin/yq https://github.com/mikefarah/yq/releases/download/v4.44.3/yq_linux_amd64
          sudo chmod +x /usr/local/bin/yq
      - name: Stamp release component provenance
        env:
          RELEASE_VERSION: ${{ needs.resolve-version.outputs.version }}
        run: |
          yq -i '.provenance.releaseVersion = strenv(RELEASE_VERSION)' helm/values.yaml
          for component in orchestrator agent cockpit mcp workspace; do
            export component
            yq -i '
              .provenance.components[strenv(component)].sourceRevision = strenv(GITHUB_SHA) |
              .provenance.components[strenv(component)].releaseVersion = strenv(RELEASE_VERSION)
            ' helm/values.yaml
          done
      - name: Package both charts
        run: |
          helm repo add collabora https://collaboraonline.github.io/online --force-update
          helm repo add cloudnative-pg https://cloudnative-pg.github.io/charts --force-update
          helm dependency build helm/
          mkdir -p dist
          for chart_dir in helm helm-vm-cluster; do
            helm package "${chart_dir}/" \
              --version "${{ needs.resolve-version.outputs.version }}" \
              --app-version "${{ needs.resolve-version.outputs.version }}" \
              --destination ./dist
          done
      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Push both charts to OCI registry
        run: |
          # Chart names are stable; use them directly to find the .tgz
          # produced by `helm package` (named <chart-name>-<version>.tgz).
          for chart_name in superhuman-remote-worker superhuman-remote-worker-vm-cluster; do
            helm push "./dist/${chart_name}-${{ needs.resolve-version.outputs.version }}.tgz" \
              oci://ghcr.io/knaeckebrothero/charts
          done
      - name: Push git tag
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git tag -a "${{ needs.resolve-version.outputs.tag }}" \
            -m "Release ${{ needs.resolve-version.outputs.tag }}"
          git push origin "${{ needs.resolve-version.outputs.tag }}"
