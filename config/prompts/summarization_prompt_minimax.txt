You are compressing a conversation for handoff to your future self. After this summary,
the raw history will be gone — this summary is the ONLY context you will have to continue working.

<exclusions>
Do NOT summarize plan.md content, knowledge base injections, or memory system injections. These are persistent and re-injected fresh each turn.
Only note ACTIONS TAKEN on plan.md (e.g., "Updated plan.md with revised phase structure") or knowledge base entries written via kb_write.
</exclusions>

<analysis>
WHY: Analyzing before summarizing prevents you from missing critical details.

Before generating the summary, work through these questions:
1. What was the user's original request? What are the success criteria?
2. What actions succeeded? What failed and why (exact error messages)?
3. Did the user correct or redirect at any point? Quote corrections verbatim.
4. What was actively being worked on when the conversation ended?
5. What specific details (IDs, paths, values, names, credentials) must survive?
6. What approaches FAILED and should NOT be retried?
</analysis>

<priority>
WHY: When you must cut details, this ordering prevents the most damaging losses.

Preservation priority (highest to lowest):
1. User corrections and constraints (verbatim quotes)
2. Errors, failed approaches, and exact error messages
3. Active work state (what was in progress at the end)
4. Completed work with verified outcomes
5. Historical context and older completed work
</priority>

<preserve>
WHY: The agent will lose ALL conversation context after compaction. These details are needed to continue without repeating mistakes or losing progress.

- Completed work: what was actually done, not what was planned
- Key decisions: choices made and WHY (the reasoning matters for future phases)
- Discovered information: entity IDs, file paths, version numbers, exact error messages
- Current state: where the agent is in the plan, what comes next
- Errors and blockers: exact error text, what was tried, what is still blocked
- Failed approaches: what did NOT work and why — prevents the agent from retrying
- Error patterns: if the same error occurred multiple times, note it explicitly
- Deliverable status: which outputs are verified vs. only claimed, with evidence
- Shell state: which tabs are open, which hosts are connected via SSH
- User corrections: direct quotes of "don't do X", "actually I meant Y"
</preserve>

<omit>
- Routine tool calls that succeeded without notable results
- Verbose outputs saved in files (reference the file path instead)
- Debugging back-and-forth that led to a solution (note the solution only)
- Planning discussions captured in the plan file
- Pleasantries, acknowledgments, filler text
</omit>

Required JSON schema:
{{
  "summary": "Concise narrative of what happened (max 3 sentences)",
  "tasks_completed": ["List of tasks finished with outcomes"],
  "tasks_in_progress": ["Tasks started but not finished, with current state"],
  "key_decisions": ["Choices made with reasoning"],
  "current_state": "One sentence: where the agent is right now and what comes next",
  "blockers": ["Active blockers with exact error messages"],
  "critical_facts": ["Exact IDs, file paths, URLs, version numbers, config values, credentials (redacted) that must survive verbatim"],
  "state_changes": ["Files created, modified, or deleted"],
  "pinned_instructions": ["Active constraints from instructions that must persist"],
  "identity_anchor": {{
    "agent_role": "Current role/persona",
    "current_task": "What the agent is working on right now",
    "active_constraints": ["Top 3-5 constraints that must survive compaction"]
  }}
}}

Conversation:

{conversation}

Keep each field concise. Total summary under {max_summary_length} tokens.
Weight recent messages more heavily — the end of the transcript is the active context.
