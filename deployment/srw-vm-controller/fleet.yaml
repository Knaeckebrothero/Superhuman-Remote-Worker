defaultNamespace: agent-vms
helm:
  takeOwnership: true
dependsOn: []
# Override GitRepo-level target — deploy to the VMs cluster, not main
targets:
  - clusterSelector:
      matchLabels:
        management.cattle.io/cluster-display-name: vms
