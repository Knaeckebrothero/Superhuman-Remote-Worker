# Project-specific contents go here
backups/*
workspace/*
./workspace/*
output/*
orchestrator/workspace/*
src/orchestrator/workspace/*
project_documentation/*
docker/certs/*

# SSH keys (generated for workspace/VM access — never commit)
.dev/
.local-ssh/

# VPN credentials (config.example is tracked, config is not)
docker/vpn/config
docker/vpn/cluster.conf
docker/vpn/research.conf

# MCP configuration files
.mcp.json
.playwright-mcp/*

# Tilt — local runtime state + downloaded extensions cache.
# The Tiltfile itself IS committed (it's the dev-loop recipe).
.tilt-state/
tilt_modules/
.tiltbuild/

# Python virtual environment
venv/
env/

# Local environment
.env

# Helm local-dev credentials overlay — values-local.yaml.example is tracked; the real one is NOT
deployment/values-local.yaml
# Generated by scripts/local-dev-up.sh: component image tags pinned to the checkout
deployment/values-local-images.yaml

# SQLite database files
*.db

# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]

# C extensions
*.so

# Distribution / packaging
bin/
build/
develop-eggs/
dist/
eggs/
lib/
lib64/
parts/
sdist/
var/
*.egg-info/
.installed.cfg
*.egg

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
.tox/
.coverage
.cache
nosetests.xml
coverage.xml

# Translations
*.mo

# Java / Spring Boot
*.class
*.log
*.ctxt
.mtj.tmp/
*.jar
*.war
*.ear
hs_err_pid*
application.properties

# JavaScript / React
logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pids
*.pid
*.seed
*.pid.lock
lib-cov
coverage
.nyc_output
.grunt
bower_components
.lock-wscript
build/Release
node_modules/
jspm_packages/
typings/
.npm
.eslintcache
.node_repl_history
*.tgz
.yarn-integrity

# Gradle
.gradle/

# NetBeans
nb-configuration.xml

# OSX
.DS_Store

# Vim
*.swp
*.swo

# patch
*.orig
*.rej

# Plugin directory
/.quarkus/cli/plugins/

# STS
.apt_generated
.springBeans
.sts4-cache

# System files
Thumbs.db

# Compiled output
/dist
/tmp
/out-tsc
/bazel-out

# Node
npm-debug.log
yarn-error.log

# Mr Developer
.mr.developer.cfg
.project
.pydevproject

# Rope
.ropeproject

# Django stuff:
*.pot

# Sphinx documentation
docs/_build/

# IDEs and editors
.project
.classpath
.c9/
*.launch
.settings/
*.sublime-workspace

# Kate — local project file, kept on disk but untracked (2026-08-17)
.kateproject
.kateproject.d/

# IntelliJ
*.ipr
*.iml
*.iws

# Pycharm
.idea
.idea/workspace.xml

# Visual Studio
.vscode
.factorypath
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
.history/*

# AI Stuff
.ai
CLAUDE.md
AGENTS.md
.claude
.junie
.codex
.agents
.playwright-mcp

# Miscellaneous
/.angular
/.angular/cache
.sass-cache/
/connect.lock
/libpeerconnection.log
testem.log
HELP.md
target/
!.mvn/wrapper/maven-wrapper.jar
!**/src/main/**/target/
!**/src/test/**/target/

# Memory eval harness — datasets are large downloads, runs are local artifacts
eval/memory/data/
eval/memory/runs/

# App Guide model-eval artifacts (the held-out corpus and harness are tracked)
eval/app_guide/runs/

# Superpowers brainstorm companion (local mockups)
.superpowers/
bench/runs/

# Extracted Better Resavio client code (Hotel Rheinland) — staged locally for
# push to a separate GitHub repo. NEVER commit client code to this public repo.
KurortEngine/
KurortEngine-salvage/
KurortEngine-extracted/
BetterResavio-KB/

# Separate repos checked out inside this working tree. Untracked is not enough —
# one `git add -A` would publish a private repo into the public one.
HomeLab/
# Knowledge vault, cloned in as a plain nested repo (not a submodule).
knowledge-base/
knowledge-history/
srw-cloud/

# Release-transition scaffolding — working docs, never published.
# Both describe the history-rewrite plan and where credentials used to live.
RECLONE-INSTRUCTIONS.md
release_transition_checklist.md
