# Neo4j Database Configuration
NEO4J_URI=bolt://localhost:7687
NEO4J_USERNAME=neo4j
NEO4J_PASSWORD=your_password_here

# CSV Requirements File Path
CSV_FILE_PATH=data/requirements.csv

# LLM API Keys
OPENAI_API_KEY=your_openai_api_key_here

# Optional: Custom OpenAI-compatible API endpoint (e.g., vLLM, Ollama, LocalAI)
# Leave unset to use OpenAI's API directly
# LLM_BASE_URL=http://your-server:8000/v1

# Optional: Alternative LLM Providers
# ANTHROPIC_API_KEY=your_anthropic_api_key_here
# COHERE_API_KEY=your_cohere_api_key_here

# Optional: Web Search (Tavily)
# Get your API key at https://tavily.com/
# TAVILY_API_KEY=your_tavily_api_key_here

# Workflow Configuration
# WORKFLOW_MODE: "chain" for simple LangChain workflow or "agent" for LangGraph agent
# - chain: Linear workflow (refine -> query -> analyze) - faster, simpler
# - agent: Iterative agent (plan -> query -> reason -> repeat) - more thorough, can explore
WORKFLOW_MODE=chain

# Note: LLM model settings (model name, temperature, max_iterations) are configured in
# config/llm_config.json - not in environment variables
