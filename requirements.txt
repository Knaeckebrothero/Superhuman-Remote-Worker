pypdf>=4.0.0
python-dotenv>=1.0.0
pandas>=2.0.0
neo4j>=5.16.0
asyncpg>=0.29.0
langchain>=0.1.0
langchain-community>=0.0.16
langchain-openai>=1.1.12,<2.0  # 1.1.12 ships PR #35281 fix for langchain#34807 (merge_dicts parallel tool call corruption); also pulls newer openai SDK
langchain-anthropic>=0.1.0
langchain-google-genai>=1.0.0
langchain-groq>=0.1.0
langgraph>=0.0.20
langchain-core>=0.1.0
langchain-text-splitters>=0.0.1  # For document chunking in document_tools
langgraph-checkpoint-postgres>=1.0.0
langgraph-checkpoint-sqlite>=2.0.0  # SQLite checkpointer for LangGraph
aiosqlite>=0.19.0  # Async SQLite driver for LangGraph checkpointing
psycopg[binary]>=3.1.0
psycopg2-binary>=2.9.0  # Required by the citation_engine package (Postgres mode)
langchain-tavily>=0.1.0
arxiv>=2.1.0
aiohttp>=3.9.0
aiohttp-socks>=0.8.0  # SOCKS5/HTTP proxy for aiohttp (research tools VPN)
openai>=1.10.0
pydantic>=2.5.0
tiktoken>=0.5.0
streamlit>=1.30.0
pdfplumber>=0.10.0
pdf2image>=1.16.0  # PDF page rendering (requires poppler system package)
Pillow>=10.0.0  # Image handling for document rendering
python-docx>=1.1.0
docx2txt>=0.8  # Required by langchain Docx2txtLoader
python-pptx>=0.6.21
openpyxl>=3.1.0
beautifulsoup4>=4.12.0
# Optional: Semantic similarity for advanced duplicate detection
sentence-transformers>=2.2.0
# Vector search for workspace semantic search (Phase 8)
pgvector>=0.2.0
langchain-postgres>=0.0.6
langdetect>=1.0.9
# Exclude fastapi 0.136.3 — OSV MAL-2026-4750 (Amazon Inspector heuristic) flags it
# for adding an undocumented `fastar` dep to the `[standard]` extra. We install plain
# fastapi (no `[standard]`) so `fastar` is never pulled, but pip-audit matches on the
# version regardless. Skip just this release; floor stays open so a clean 0.136.4+ is
# picked up automatically. Tighten to `<0.136` if upstream confirms a real compromise.
fastapi>=0.109.0,!=0.136.3
uvicorn>=0.27.0
httpx>=0.26.0
# Retry primitives for the main-cloud backend layer (transitive via
# langchain-core today; listed explicitly so cloud/retry.py keeps working
# if langchain drops it).
tenacity>=8.2.0
# OpenTelemetry API for the cloud backend instrumentation decorator.
# Using the API package only — SDK/exporter installation is an ops choice.
opentelemetry-api>=1.20.0
python-multipart>=0.0.6
# Optional: MongoDB for LLM request archiving
pymongo>=4.6.0
webdavclient3>=3.14.0
# System metrics for agent heartbeats
psutil>=5.9.0
# Optional: Claude Code delegation tool (requires Claude Code CLI)
claude-agent-sdk>=0.1.35
# Template rendering for instruction files (conditional tool references)
Jinja2>=3.1
# Remote workspace backend (SSH/SFTP to VM)
paramiko>=3.4.0
# AES-GCM encryption at rest for stored credentials (user/system API keys,
# custom LLM endpoint keys). Already a transitive dep via paramiko; listed
# explicitly so crypto.py keeps working if that changes.
cryptography>=42.0
# Optional: NATS for cross-cluster VM lifecycle management
nats-py>=2.9.0
# Cron expression parsing for the automations dispatcher
# (services/cron_dispatcher.py). Pairs with zoneinfo (stdlib) for
# DST-aware next-fire-time computation against IANA-named timezones.
croniter>=2.0.0
# Runtime deps of the citation_engine package (src/citation_engine/).
# Its other deps (beautifulsoup4, langchain-openai/-core, langgraph, tiktoken,
# httpx, psycopg2-binary, docx2txt, pydantic) are already pinned above.
pymupdf>=1.23.0
requests>=2.31.0
