# =============================================================================
# Graph-RAG Autonomous Agent System - Dependencies
# =============================================================================

# Core dependencies
python-dotenv>=1.0.0
pandas>=2.0.0

# Neo4j database driver
neo4j>=5.16.0

# PostgreSQL async driver (for shared state)
asyncpg>=0.29.0

# LangChain framework for LLM orchestration
langchain>=0.1.0
langchain-community>=0.0.16
langchain-openai>=0.0.5
langchain-experimental>=0.0.49

# LangGraph for agent-based workflows
langgraph>=0.0.20
langchain-core>=0.1.0
langgraph-checkpoint-postgres>=1.0.0  # PostgreSQL checkpoint storage

# Web search for AI agents
langchain-tavily>=0.1.0

# LLM providers (OpenAI as default, can be swapped for other providers)
openai>=1.10.0

# Utilities
pydantic>=2.5.0
tiktoken>=0.5.0

# Optional: Additional LLM providers (uncomment if needed)
# anthropic>=0.18.0  # For Claude models
# langchain-anthropic>=0.1.0
# cohere>=4.0.0  # For Cohere models

# Dashboard
streamlit>=1.30.0

# Document processing for ingestion pipeline
pdfplumber>=0.10.0          # PDF text extraction
python-docx>=1.1.0          # DOCX text extraction
beautifulsoup4>=4.12.0      # HTML parsing

# Optional: Semantic similarity for advanced duplicate detection
# sentence-transformers>=2.2.0

# Language detection
langdetect>=1.0.9

# =============================================================================
# FastAPI for agent containers (Phase 5)
# =============================================================================
fastapi>=0.109.0
uvicorn>=0.27.0
httpx>=0.26.0  # Async HTTP client for health checks
python-multipart>=0.0.6  # For FastAPI file uploads

# =============================================================================
# Citation Engine (local development)
# =============================================================================
# Install the citation engine from local directory:
# -e ./citation_tool[full]
#
# Or from git (when available):
# citation-engine[full] @ git+https://github.com/youruser/citation-tool.git@main
