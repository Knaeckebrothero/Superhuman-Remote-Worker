{{- if include "srw.vmSameCluster" . }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "srw.vmControllerName" . }}-template
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "srw.componentLabels" (dict "context" . "component" "vm-controller") | nindent 4 }}
data:
  vm-template.yaml: |
    apiVersion: kubevirt.io/v1
    kind: VirtualMachine
    metadata:
      name: agent-vm-${JOB_ID}
      labels:
        vm.kubevirt.io/name: agent-vm-${JOB_ID}
        job-id: ${JOB_ID}
        srw.io/owner-kind: "${OWNER_KIND}"
        srw.io/owner-id: "${OWNER_ID}"
    spec:
      runStrategy: RerunOnFailure
      dataVolumeTemplates:
        - metadata:
            name: agent-vm-${JOB_ID}-rootdisk
            labels:
              srw.io/owner-kind: "${OWNER_KIND}"
              srw.io/owner-id: "${OWNER_ID}"
          spec:
            storage:
              accessModes: [ReadWriteOnce]
              storageClassName: ${VM_STORAGE_CLASS}
              resources:
                requests:
                  storage: ${VM_DISK_SIZE}
            source:
              registry:
                url: docker://${VM_IMAGE}
      template:
        metadata:
          labels:
            vm.kubevirt.io/name: agent-vm-${JOB_ID}
            job-id: ${JOB_ID}
            srw.io/owner-kind: "${OWNER_KIND}"
            srw.io/owner-id: "${OWNER_ID}"
            srw.io/component: agent-workspace
            srw.io/network-tier: "${NETWORK_TIER}"
        spec:
          readinessProbe:
            tcpSocket:
              port: 22
            initialDelaySeconds: 30
            periodSeconds: 5
            failureThreshold: 60
          domain:
            cpu:
              cores: ${CPU_CORES}
            memory:
              guest: ${MEMORY}
            devices:
              disks:
                - name: rootdisk
                  disk: {bus: virtio}
                - name: cloud-init
                  disk: {bus: virtio}
              interfaces:
                - name: default
                  masquerade: {}
            machine: {type: q35}
          networks:
            - name: default
              pod: {}
          volumes:
            - name: rootdisk
              dataVolume:
                name: agent-vm-${JOB_ID}-rootdisk
            - name: cloud-init
              cloudInitNoCloud:
                secretRef:
                  name: agent-vm-${JOB_ID}-cloudinit
  cloud-init.yaml: |
    #cloud-config
    write_files:
      - path: /etc/default/srw-guest
        permissions: '0600'
        owner: root:root
        content: |
          ORCHESTRATOR_URL=${ORCHESTRATOR_URL}
          VM_AUTH_TOKEN=${VM_AUTH_TOKEN}
          ENTITY_ID=${JOB_ID}
          ENTITY_TYPE=${OWNER_KIND}
          JOB_ID=${JOB_ID}
          VM_ID=agent-vm-${JOB_ID}
      - path: /run/agent/job-config.json
        permissions: '0644'
        content: |
          {
            "job_id": "${JOB_ID}",
            "agent_config": "${AGENT_CONFIG}",
            "cpu_cores": ${CPU_CORES},
            "memory": "${MEMORY}"
          }
      - path: /etc/ssh/authorized_keys/agent-host
        permissions: '0600'
        owner: agent-host:agent-host
        content: |
          ${SSH_AUTHORIZED_KEY}
    runcmd:
      - mkdir -p /run/agent
      - chmod 755 /run/agent
      # The controller injects ssh_keys into the per-VM Secret at render time.
      # Regenerating here would replace the orchestrator-pinned identity.
      - systemctl restart ssh
      - systemctl daemon-reload
      - systemctl enable management-daemon.service
      - systemctl start management-daemon.service
      - systemctl restart sudo-gated.socket
{{- end }}
