# GENERATED FIXTURE — do not hand-edit.
#
# This is the self-host config generator's verbatim output for the "production-vms" profile.
# The generator itself lives in the PRIVATE srw-cloud repo (www/generator.mjs);
# it moved out of this repo on 2026-08-15. These fixtures are the surviving half
# of the contract: they prove a chart change has not broken the shape of values
# the installer emits, and they fail HERE, in the repo that caused the change,
# rather than days later in srw-cloud's CI.
#
# If chart-test fails on this file, the chart changed in a way the installer does
# not yet produce. Fix the generator in srw-cloud, then regenerate these from its
# www/test/generator.drift.test.mjs CASES.
#
# What this CANNOT catch: a change to the generator itself. Only srw-cloud's
# drift gate sees that. Neither half is sufficient alone.
# Generated by the SRW config generator — review every value before installing.
# Profile: production-vms
license:
  acceptTerms: true

global:
  domain: "srw.example.com"

secrets:
  existingSecret: srw-secrets

keycloak:
  enabled: true
  internal: false
  externalIssuerUrl: "https://login.example.com/realms/x"
  cockpitClientId: "srw-cockpit"
  adminApiDisabled: true

gitea:
  enabled: true
  internal: false
  internalUrl: "https://git.example.com"
  externalUrl: "https://git.example.com"

opencloud:
  enabled: false
nextcloud:
  enabled: false
cloud:
  externalBackend: "nextcloud"
  externalUrl: "https://cloud.example.com"
  externalServiceUrl: "https://cloud.example.com"

databases:
  postgres:
    enabled: true
    internal: false
    externalHost: "pg.example.com"
    externalPort: 5432
    externalDb: "srw"
  vector:
    enabled: true
    internal: false
    externalHost: "pg.example.com"
    externalPort: 5432
    externalDb: "srw_vector"
  mongodb:
    enabled: true
    internal: false
    externalUrl: "mongodb://mongo.example.com:27017/srw_logs"
  neo4j:
    enabled: false

ingress:
  enabled: true
  className: nginx
  tls:
    enabled: true
    issuerName: letsencrypt-prod

vmController:
  enabled: true
  transport: http
  namespace: agent-vms
  vmStorageClass: longhorn
  vmDiskSize: 30Gi
  vmSshPublicKey: "ssh-ed25519 AAAA test@srw"

