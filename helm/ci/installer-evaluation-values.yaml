# GENERATED FIXTURE — do not hand-edit.
#
# This is the self-host config generator's verbatim output for the "evaluation" profile.
# The generator itself lives in the PRIVATE srw-cloud repo (www/generator.mjs);
# it moved out of this repo on 2026-08-15. These fixtures are the surviving half
# of the contract: they prove a chart change has not broken the shape of values
# the installer emits, and they fail HERE, in the repo that caused the change,
# rather than days later in srw-cloud's CI.
#
# If chart-test fails on this file, the chart changed in a way the installer does
# not yet produce. Fix the generator in srw-cloud, then regenerate these from its
# www/test/generator.drift.test.mjs CASES.
#
# What this CANNOT catch: a change to the generator itself. Only srw-cloud's
# drift gate sees that. Neither half is sufficient alone.
# Generated by the SRW config generator — review every value before installing.
# Profile: evaluation
license:
  acceptTerms: true

global:
  domain: "eval.example.com"

# Eval/single-node: chart-created secrets (dev/eval only).
secrets:
  create: true
  values: {}

databases:
  neo4j:
    enabled: false

ingress:
  enabled: true
  className: traefik
  tls:
    enabled: false

